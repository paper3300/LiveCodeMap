# LiveCodeMap

LiveCodeMap is a planned Windows-native code exploration and context layer for C++ and Unreal Engine projects. Product requirements and design decisions currently live in `Workspace/Docs/`.
