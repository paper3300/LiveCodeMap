# First release execution plan

**2026-09-25 next-stage checkpoint:** Parallel-build diagnosis and two additional Temppal TU replay pairs are complete within the selected existing-input snapshot scope, after GPT-6-Sol/high independent review. The only product change exports existing file/include observations in `lcm_replay`; analyzer semantics are unchanged. See [results and limitations](2026-09-25-Next-Stage-Execution.md) and the [next TU contribution/freshness contract](2026-09-25-Multi-TU-Index-Handoff.md). SQLite storage, query APIs and MCP remain unimplemented; broader Phase 1 and release gates remain open.

**2026-09-22 real-project selection:** the user designated `D:/ProjectT_Main/Program/Client/Temppal` as the first Unreal test and product motivation. Coordinator inspection confirms the adjacent source engine at `D:/ProjectT_Main/Program/Client/Engine` reports UE 5.7.4. The initial selected profile is `TemppalEditor / Win64 / Development / x64`, corroborated by its existing build receipt; DebugGame and game targets remain distinct. Start with read-only context assessment, then freeze actual source/generated-header/response/PCH/unity provenance before analyzer validation. Use project navigation and impact-analysis needs to shape representative test tasks. Existing build outputs do not establish freshness or LiveCodeMap acceptance. This user project does not replace the separate public benchmark evidence. Details and remaining gaps: [current recovery record](2026-09-22-Progress-and-Resume.md).

**Fresh-session restart at the user's request, 2026-09-18 19:39 KST; status updated after the 20:36 review.** Coordinator `w8:p6` recovered current files and the [progress and resume report](2026-09-18-Progress-and-Resume.md). The user superseded reuse of the old sessions: new Claude implementation agent `lcm-impl-fresh` (`w8:p7`) and new Codex reviewer `lcm-review-fresh` (`w8:p8`) run in tab `w8:t4`, without resume/fork or prior conversation context. The old three panes remain idle. Bounded Phase 1C is now accepted following the fresh Declaration correction review (27 runs, 1,002/1,002 checks). The direct-base/include increment below is activated. Full Phase 1 and all whole release gates remain incomplete. Earlier entries below retain their historical evidence boundaries.

Baseline: `LiveCodeMap-PRD.md` v0.3. This plan does not replace or relax its requirements.

The coordinator-owned [first-release acceptance ledger](First-Release-Acceptance-Ledger.md) indexes all FR/SEC families, required surfaces/artifacts and full release evidence. Its gates remain incomplete until verified at their actual PRD scope; bounded increment acceptance does not imply release acceptance.

## Ownership

| Agent | Role | Ownership |
| --- | --- | --- |
| agent1, current coordinator | Plan, resolve requirements, integrate evidence | This execution plan and release decisions |
| agent2, `lcm-impl-fresh`, new Claude session | Implementation | Product code, build files, fixtures, tests and implementation documentation; exclusive main build-tree owner |
| agent3, `lcm-review-fresh`, new Codex session | Independent review | Product files read-only; independent probes in `build/reviewer-fresh`; final verification after freeze |

All agents use the same working directory through Herdr sibling panes. Only agent2 edits implementation files. Existing untracked planning documents belong to the user and must be preserved. No additional agents are assigned.

## Starting evidence

- Repository initially contains planning documents, shared agent instructions and ignore rules; no product implementation or tests.
- Windows has Visual Studio 2022 Professional with MSVC 14.44, CMake 3.30.0, Node 24.16.0 and Python 3.11.
- LLVM/Clang development libraries have not been located in the inspected standard installation paths. Dependency provisioning remains necessary to verify semantic analysis.
- PRD requires separate ordinary C++ and Unreal validation. Synthetic substitutes cannot establish real UBT/UHT compatibility.
- No release gate is currently verified.

## Implementation baseline under review

Use a native C++20 core with CMake presets, a Clang semantic analyzer, SQLite storage and shared CLI/MCP response types. Keep analysis adapters separate from identity, storage and query logic. This follows the PRD's native binary/source-build and compiler-evidence requirements; the precise pinned Clang distribution must be verified before selection.

Compile commands are input data, never shell commands. Preserve original arguments and normalization diagnostics. Analysis failure must retain committed facts and report partial/stale/missing state. Do not replace compiler resolution with name matching.

## Work sequence and acceptance

| Phase | Deliverable | Required verification before acceptance |
| --- | --- | --- |
| 1: IR and adapters | Buildable native skeleton; canonical identities; source locations/hashes; compilation database adapter; semantic facts and unresolved evidence; Unreal context boundary | Clean build, C++ synthetic gold relations, declaration/definition merging, stable identity mutations, ambiguous commands, no-context fallback; real UE synthetic lane separately |
| 2: Index and freshness | Normalized SQLite contribution store; atomic replacement; dependency invalidation; OS writer lock; per-layer state | Mutation gold set including ignored/external dependencies, rollback/crash behavior, independent TU evidence, profile changes, stale budgets |
| 3: Tools and early A/B | Shared CLI/MCP model; retrieval, file API, callers/trace/map/blast; cursor and snippet contracts | CLI/MCP parity; cyclic/high-fan-out pagination without omissions; source-hash snippet checks; development retrieval set; Navigation/Impact/Patch paired A/B |
| 4: Retrieval quality | Identifier/path fields, lexical ranking and graph projection, workspace fusion | Fixed development set and comparable Graft 0.18.0 evaluation; investigate regressions; preserve untouched holdout |
| 5: Optional deep | Explicit provider invocation, symbol-window input, content cache, independent failure and usage | Real provider usage; unchanged rerun makes no provider calls; limits and partial failures; structural queries work independently |
| 6: Integration and release | Safe init/remove and hooks; bounded viewer/export; npm/ZIP/mcpb packaging; CI and notices | Host preservation/idempotency tests, viewer scope/security checks, clean-machine smoke, full holdout/A/B, package allowlist and every PRD section 13 gate |

Each bounded implementation assignment must name its scope and tests. The coordinator checks its results and requests independent review. Actionable defects return to agent2; affected checks run again. A phase remains incomplete when required external evidence is unavailable.

### Foundation acceptance cases

The first assignment is only a prerequisite to Phase 1; passing it does not establish semantic graph support.

- Identity: external named symbols retain IDs across repository relocation, file move, line insertion, comment/body edits and active-profile changes. Owner, name, normalized signature, kind, language and repository-member changes distinguish identities. Internal symbols distinguish repository-relative file scopes. Serialization cannot conflate delimiter-containing components. Clang USR remains evidence only.
- Source evidence: spans refer to the original file bytes and carry their content hash. Unicode path/source round trips preserve spelling. Hash output is deterministic across repeated runs; no implementation-dependent `std::hash` as a persistent ID.
- Compile database: cover `arguments` and Windows-quoted `command`, relative `directory`/`file`, optional `output`, spaces and Unicode, malformed input and multiple commands for one source. Command text is never executed. Raw and normalized arguments and normalization diagnostics remain distinguishable. Missing context has an explicit state and does not imply Unreal is required.
- Graph/status model: confirmed, possible and unresolved evidence are distinct; unresolved callsites need no invented target. Multiple evidence contributions can refer to one logical relation. General C++ UHT is not applicable and does not degrade overall status.
- Build: configure and build with a documented CMake preset, execute actual behavior tests, report exact command results and dependency versions. Missing compiler SDK is explicit; tests that require it cannot silently become passing mocks.

### Subsequent review boundaries

Before accepting a phase, the independent reviewer must inspect actual code and test coverage, including negative inputs. The coordinator can perform preliminary checks and continue prerequisite implementation while the requested review model is unavailable, but cannot claim agent3 review occurred or accept the phase without it. Future work is accepted against the complete PRD requirement, not merely against the initial cases above.

## Decisions and external inputs pending

- Claude's initial workspace trust dialog was cleared externally; Herdr now confirms an interactive-ready Fable 5.1 / high session. The coordinator did not answer the dialog.
- Resolved: the user explicitly corrected `gpt-6-sol` to `gpt-5.6-sol`; Herdr confirms the `gpt-5.6-sol` / high reviewer is working on the foundation audit.
- Supported Unreal version, engine location, public fixture target/configuration and real UE runner.
- Provider/model and usage budget for repeated Agent A/B and optional deep experiments.
- Verify and pin compiler dependencies, public corpus commits and benchmark split manifests before tuning.
- Verify npm organization ownership and release CI/OIDC setup before publication. No release is published by this plan alone.

## Release evidence policy

Public source revisions resolved with `git ls-remote` on 2026-09-18:

| Source | Requested tag | Pinned commit |
| --- | --- | --- |
| `https://github.com/fmtlib/fmt.git` | `12.2.0` | `1be298e1bd68957e4cd352e1f676f00e07dcfb57` |
| `https://github.com/CesiumGS/cesium-unreal.git` | `v2.29.1` | `29ea626a7a5c76f12e49fd22e9628c870740598d` |
| `https://github.com/trailhq/Graft.git` | `v0.18.0` | `de8456e892bad5aeee11403e47fb2227773eb27e` |

Annotated Cesium and Graft tags were dereferenced to commits. This verifies remote revision identity only; corpus checkout, submodule pinning, build/profile preparation, benchmark manifest and query/gold-set validation are still outstanding.

Track implementation, local test evidence, real-environment evidence and publication readiness separately. A stub, skipped test, missing provider run or mock UE artifact is not a passed release gate. Never infer token savings from text length. Do not modify PRD numerical thresholds without the user decision and benchmark evidence required by PRD section 19.

Only public synthetic/open-source inputs may appear in public fixtures or reports. Private local corpus identities, paths, configuration, source, derived facts and measurements stay outside public artifacts.

## Current status

The bounded foundation, pinned LLVM SDK prerequisite, ordinary-C++ Phase 1A/1B and adapter correction are accepted after independent correction/re-review cycles. Agent3 accepts the adapter at 16:40 after the duplicate-input fix: fresh probes have zero failures and all 50 C++ contexts remain gate-ready with their identities/profiles preserved. The separate explicitly selected corpus frontend-readiness survey completes 50/50 without compiler errors/timeouts; it retains warnings, limitations and unresolved evidence and is independently checked at 16:47. Phase 1C's initial 17:19 handoff passed coordinator reruns (core 102/2,438, analyzer 22/2,132 and smoke 8/67 in all three configurations; Clang-disabled core 102/2,438 and CTest 3/3), but independent review reproduced two High identity blockers. Agent2 is now correcting unsupported-wrapper raw fallback and typed-null template-argument collisions; Phase 1C remains unaccepted. Remaining ordinary-C++ coverage, all six full PRD phases and the first public release remain incomplete.

Coordinator preliminary checks during implementation (not independent agent3 review):

- Requested an explicit no-op for empty streaming hash updates and independent SHA-256 boundary vectors.
- Identified local-symbol collisions when equal static-function owner chains occur in different source files; requested enclosing stable identity in the local discriminator and cross-file regression coverage. Implementation is being revised.
- Identified Windows narrow filesystem conversions in source paths; requested UTF-8 round trips and real non-ASCII path tests. Implementation is being revised.
- Identified surviving relation evidence whose indexed endpoint could be erased by another TU's contribution removal; requested truthful endpoint preservation and a regression without discarding surviving evidence.
- Identified `availability=ready, freshness=unknown` summarizing as `ready`; requested a non-ready summary until freshness is established and an explicit regression.

Initial foundation evidence: Claude reported 50 behavior tests / 547 assertions passing in Debug and Release. The coordinator independently ran both `ctest --preset debug` and `ctest --preset release`, each passing its `lcm_core_tests` executable with no failed test. Thirteen SHA-256 block-boundary expected answers were separately recomputed with .NET and matched. Preliminary fixes were present, but the subsequent independent audit below found additional defects. This evidence covers only the foundation, not compiler semantic extraction or any whole release gate.

Next prerequisite: prepare official LLVM 22.1.0 Windows x64 LibTooling SDK in the ignored project-local build tree and verify a real Release-mode Clang AST smoke executable. Official GitHub API asset: `clang+llvm-22.1.0-x86_64-pc-windows-msvc.tar.xz`, 861897432 bytes, SHA-256 `2dd6a6c990865f98766dec58cce1cbfba48e5affc0098e5b1fff96c2f1d23e81`. Disk capacity was checked. The coordinator authorizes this ordinary required dependency preparation under the existing implementation request; no machine-wide installation or LLVM source build is planned.

### Independent foundation review: corrections required

Agent3 completed its first audit with `gpt-5.6-sol` / high. It reran both existing configurations and used isolated MSVC probes under `build/reviewer`; source files were not edited. Passing the original 50 tests did not cover these findings, so foundation acceptance remains open:

| Finding | Required correction and evidence |
| --- | --- |
| Surviving cross-TU unresolved sites lose their enclosing symbol on contribution removal | Include unresolved owners in liveness; cross-unit removal and final cleanup regression |
| Internal IDs vary for equivalent lexical/Windows path spellings | Enforce canonical relative scope and use canonical identity for existing Windows files while preserving display spelling; dot-segment and real Unicode case-variant regressions |
| Invalid/incomplete canonical keys are accepted, while anonymous namespace nodes cannot be represented | Validate callable/owner signatures and kind/linkage combinations; allow correctly file-scoped anonymous namespaces; positive/negative tests |
| Unterminated command quoting remains selectable and can merge with valid command identity | Explicitly reject unusable command entries with indexed diagnostics; keep valid neighboring entries; malformed/valid mixed selection tests |
| Raw/normalized argv and dropped-option reasons are not implemented | Implement the originally planned compile-context normalization contract and verify preservation of raw input, normalized output and per-option reasons; keep acceptance open until present |
| Direct parse API accepts a relative database base and returns relative command paths | Require or normalize an absolute base; direct parse regression, not only file-loading tests |

### LLVM SDK prerequisite: local evidence, independent review pending

The SDK bootstrap completed with the pinned SHA-256 and an OpenPGP signature verified against LLVM's HTTPS-fetched release keys. The downloaded Sigstore attestation was inspected but not cryptographically verified; these are distinct claims. The archive was installed only under the ignored project-local `build/deps` tree. Bootstrap rerun was a no-op after checking the marker and required SDK paths.

The SDK smoke reported seven real-Clang tests / 58 assertions passing in Debug, Release and RelWithDebInfo. The coordinator independently reran all three smoke executables and confirmed 7/58 passing in each; the emitted compiler error is the intentional broken-TU negative case. CTest passed both core and smoke executables in each configuration in the worker's run. Separate missing-SDK/required-SDK and Clang-disabled checks were exercised. Details and exact limits are in the local `build/reports/clang-sdk-handoff.md`; agent3 is independently reviewing acquisition, CMake integration and smoke-test evidence.

Verified ABI: LLVM 22.1.0 Windows MSVC libraries use the static release CRT (`/MT`), RTTI OFF and assertions OFF. The SDK's embedded VS Enterprise DIA library path was remapped to the installed VS instance. Debug smoke uses the release CRT and iterator-debug level zero; it is not a debug LLVM SDK. The smoke library does not link the core. Correction to the initial handoff: the official `LLVMConfig.cmake` line 22 sets `CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded` globally, and the current SDK-enabled core project actually uses `/MT` in every configuration; Clang-disabled scratch builds still use the default `/MD[d]`. The product analyzer must make compatible CRT settings explicit and verify the combined link, rather than rely on this import side effect. The smoke does not establish that integration.

### Next bounded implementation: compilation context to product facts

After foundation corrections, implement a real ordinary-C++ analyzer path that consumes selected compile context and produces the common `FactSet`, not a second smoke-only fact model. The initial increment covers real disk input, headers in their consumer TU context, canonical declaration/definition merging, owner-correct namespace/record/callable symbols, direct calls and unresolved indirect sites, original-byte source evidence, and direct include/dependency observations. Further C++/Unreal semantic increments remain explicit pending work.

Acceptance for this increment: fixture-driven compilation-database analysis, distinct overload/owner IDs, cross-TU header evidence, internal-scope separation, compile-define context differences without arbitrary command selection, unchanged IDs after external-symbol file moves/body edits, no false targets for comments/strings/indirect calls, actionable malformed/missing-include diagnostics, and deterministic facts over three runs. Unimplemented semantics must remain limited/unresolved, never invented confirmed facts. External declarations may resolve references but only referenced placeholders are retained, without indexing external bodies or paths. Source input commands are never executed. Product/core CRT integration and the Clang-disabled build must both be tested.

### Foundation correction handoff: re-review pending

Claude completed the six correction items and the coordinator's additional pre-test findings. Debug and Release now each pass 73 tests / 1,112 assertions; the coordinator independently executed both updated core binaries. The worker also reports both CTest executables passing in each configuration. The local `build/reports/foundation-corrections.md` records the changes and limitations. This does not close the audit until agent3 re-verifies the corrected behavior and new normalizer.

Additional coordinator findings corrected in this round: `-Wp,` preprocessor arguments must not be discarded as warnings; joined GNU output options and unsupported separated MSVC forms need explicit treatment; `/Tp` and `/Tc` must affect analyzer invocation; named namespaces under anonymous namespaces and named local types must remain representable. Clang's actual driver expansion independently confirmed the `-Wp,-D...` semantic input.

While agent3 completes SDK review and foundation re-review, agent2's next assignment is restricted to new analyzer/interface/fixture/test files. Existing foundation source/tests and SDK/CMake integration stay frozen until the coordinator relays review completion or specific corrections. The analyzer must also reject unsupported compiler-plugin execution/output side effects rather than treating arbitrary frontend options as permission to execute project code.

### Independent SDK review: corrections required

Agent3 independently confirmed the archive size/SHA-256, pinned OpenPGP signer, `/MT` and iterator-debug ABI, three 7/58 smoke runs, and honest missing-SDK behavior. It reported four actionable gaps, accepted for correction:

| Finding | Required correction |
| --- | --- |
| `-Force` uses resume and cannot replace a same-size corrupt cached download | Force a fresh transfer; bounded corruption recovery; tiny local complete/partial corruption tests |
| Smoke visitor duplicates default-argument calls and assigns them to the declared function | Enforce the documented function-body caller boundary or precise default-argument semantics; declaration/definition regression; avoid carrying this bug into the analyzer |
| SDK discovery calls any same-version override pinned while assuming official ABI | Distinguish bootstrap provenance from content verification; validate required SDK files; reject or explicitly label and ABI-check custom overrides |
| Recursive removal checks the leaf but not junction ancestors | Validate actual containment or reject reparse ancestors before destructive work; nondestructive, project-contained junction tests |

The coordinator released SDK/bootstrap/smoke files for these fixes. Existing foundation source/tests remain frozen during foundation re-review. The full SDK archive need not be downloaded again for small cache-regression fixtures. No destructive junction probe or full re-extraction was performed by the reviewer.

Phase 1A design feedback: the batch API must require explicit command selection/scope rather than merge every compilation-database entry by default. TU results should be staged or returned independently; failed/rejected analysis must not mutate prior accepted facts. Frontend passthrough must have a conservative safety boundary, including clang-cl wrapper spellings, rather than relying on a short plugin blacklist or `-fsyntax-only` to guarantee absence of side effects. The implementation design is recorded locally in `build/reports/phase1a-design.md` and is being revised accordingly.

### Foundation re-review result

Agent3 accepted five of the original six findings: unresolved-owner liveness, canonical internal path identity, key validation, malformed quote rejection and absolute parse base enforcement. It independently built and ran the frozen core in an isolated tree, with Debug and Release each passing 73 tests / 1,112 assertions. Source/test hashes stayed unchanged during review. Normalizer acceptance remains open for three reproduced defects:

- A value-taking option can consume the only entry-source token (`clang -D src/a.cpp`) while status remains `ok`; later appending the source invents a usable context absent from the original command.
- Empty passthrough values such as `-Wp,-D,`, `-Wp,-U,` and `-Wp,-I,` can produce bare flags with `ok` status and consume subsequently appended source arguments.
- A real Windows non-ASCII case alias of the entry source is not recognized by the normalizer's lexical-only comparison and remains as an extra input argument.

The coordinator assigned focused corrections/regressions and lifted the foundation freeze only for those items. SDK corrections retain a separate handoff, followed by normalizer re-verification and the explicitly scoped analyzer increment. The bounded foundation is 5/6 accepted, not complete.

### SQLite prerequisite for the later storage increment

The coordinator verified the official SQLite 3.53.4 amalgamation metadata on 2026-09-18 and downloaded only the source ZIP into ignored `build/deps/downloads/sqlite-amalgamation-3530400.zip`. No storage code, extraction, schema or build integration has been added.

- Official source: `https://www.sqlite.org/2026/sqlite-amalgamation-3530400.zip`; 2,946,650 bytes.
- Official SHA3-256: `628a44cfe82c66aed1ccbbe85a562d2e33ebe64b3288981ed76285612227934e`; independently recomputed from the download and matched.
- Computed SHA-256 for future CMake pinning: `1e71ddf93849c6a6ecf58b827c0692073d2dd7ee40196158068f7b29f422e87d`.
- Source identity from the [official 3.53.4 release record](https://www.sqlite.org/releaselog/3_53_4.html): `2026-07-24 19:02:57 bf7c7f30031888f4e796e429ab3978879485813aaca6f641c7b33e4e09459bcc`.

The later storage design must respect [SQLite WAL limitations](https://www.sqlite.org/wal.html): one writer, same-host shared memory, bounded readers/checkpoints, and deliberate durability settings. This is dependency preparation only; transaction, crash, concurrency, freshness and performance gates remain unverified.

### Independent semantic fixture design

Agent3 supplied four focused compiler-semantic fixture contracts (design evidence, not passing implementation tests):

| Case | Required positive behavior | Required negative assertion |
| --- | --- | --- |
| `calculate(int value = seed())`, redeclared definition, default and explicit callers | One calculate ID; default caller calls both calculate and seed; one seed-call evidence at the actual default use | No calculate-to-seed body edge; explicit argument caller does not call seed |
| `[x = make_value()] { return consume(x); }()` | Enclosing caller calls make_value and the lambda; lambda calls consume; init-capture and immediate invocation metadata retained | No enclosing-to-consume or lambda-to-make_value edge; no separate public closure operator() |
| Base virtual, final Derived override, explicit `Base::run()` | Confirmed selected slot for base dispatch; confirmed final/qualified targets; compiler override and direct base facts | No stored confirmed base-dispatch-to-Derived implementation edge; runtime candidates are query-time possible expansion |
| `template<class T> int token()` plus explicit `token<int>()` and implicit double use | Distinct primary/specialization IDs; int calls specialization; implicit double use folds to primary with type-argument/provenance metadata | No ID collision despite both parameter lists being `()`; no implicit double public node or cross-assigned evidence |

The last case exposes an explicit IR gap in the preliminary analyzer design: template role/arguments and per-call metadata must be represented before that semantic contract can be accepted. Unsupported constructs must remain explicit limitations/unresolved evidence until implemented, not be merged by name or converted into incorrect confirmed relations. These cases do not replace the complete PRD template, callable, type-wrapper or Unreal requirements.

SDK test-isolation incident check: fixture cleanup used junctions into the live SDK, so the coordinator stopped repeated cleanup and requested isolated fixtures without such links. Read-only checks found every manifest-required SDK path present and the original 861,897,432-byte archive SHA-256 unchanged. No live SDK damage was established and no recovery extraction was authorized on speculation. Test fixture cleanup and original SDK containment must be re-verified before SDK acceptance.

SDK correction handoff is now `build/reports/sdk-corrections.md`: worker reports Debug/Release CTest 4/4, bootstrap 33 checks, discovery 18 checks, and updated smoke 8 tests / 67 assertions. The coordinator independently reran the Release smoke and verified 8/67 passing. Actual override linking now tests incompatible `/MD` libraries and compatible genuine SDK linkage. Agent3 is re-reviewing the four correction items and fixture-cleanup safeguards. SDK file-list parity is evidence of no missing/extra paths, not a cryptographic verification of every extracted byte.

SDK re-review accepted download recovery and smoke attribution; production ancestor guard is accepted but test cleanup remains unsafe for a junction at `WorkRoot` itself or above it. Test reset must validate the full build-to-work-root chain before recursive enumeration/deletion. The `/MD` negative fixture also lacked the exact probed function, so its failure did not prove ABI rejection. Required correction: identical probe-symbol implementations in `/MT` positive and `/MD` negative stubs, with RuntimeLibrary `LNK2038` specifically verified for the negative. Earlier apparent ABI-test success is therefore insufficient evidence.

Override policy clarified: an SDK's explicit CRT declaration is not independently required by the PRD. Provenance remains unverified; incompatible declarations are rejected when supplied, and actual compatible link/run is required. An undeclared but genuinely compatible `/MT` SDK may pass, provided documentation says so. Do not silently tighten the product contract solely to match a test. These SDK blockers are assigned separately after normalizer handoff.

Normalizer follow-up: worker reported 78 tests / 1,279 assertions in Debug and Release; coordinator and reviewer each reran Release with that result. Source-presence validation and operand preservation, empty passthrough handling, and `/Tp`/`/Tc` regressions were accepted. The remaining file-identity defect is a fast-path ordering issue: ASCII-folded lexical equality must not override filesystem inequality for existing case-distinct Windows files. A focused correction and a per-directory case-sensitive fixture (where supported) are assigned; no system-wide setting change is authorized.

Latest SDK test logs now show canary preservation for work-root and ancestor junctions, exact-symbol `/MD` rejection specifically by RuntimeLibrary `LNK2038` rather than unresolved symbols, compatible `/MT` positive controls, and successful real-SDK override probing. The final focused re-review is in progress; this log evidence alone is not SDK acceptance.

SDK prerequisite accepted after independent final re-review: agent3 closed the remaining reset-helper and actual-ABI-test blockers by inspecting frozen code and matching logs. All four original SDK findings are now closed within the bounded acquisition/integration/smoke scope. No full LLVM-library matrix, extracted-tree cryptographic attestation, complete analyzer semantics or clean-machine release gate is implied.

The final normalizer ordering correction reports 79 tests / 1,298 assertions in Debug and Release; the coordinator independently reran Release with the same result. The Windows per-directory case-sensitive regression executed rather than taking its unsupported-platform limitation path. Agent3 is checking only this final correction before foundation acceptance.

Foundation accepted: agent3 closed the final ordering finding and independently ran its real Windows case-sensitive regression (1 test / 19 assertions, not skipped). All six original foundation findings and their follow-ups are closed within the bounded foundation scope. The coordinator released the implementation freeze and assigned Phase 1A integration of the existing analyzer drafts, with required CMake hooks and minimal necessary IR extensions. The draft analyzer is not yet a tested product feature. Missing `source_identified`, unexpanded semantic input, failed TU analysis and unsafe options must not turn into a falsely verified context or mutate prior accepted facts.

### Storage implementation acceptance prepared for Phase 2

This is a future assignment boundary, not authorization to bypass the outstanding analyzer review. Use normalized SQLite rows for workspace/profile, units, files, symbols, locations, relations, evidence, unresolved sites, contributions and diagnostics; do not store the entire graph or source bodies as JSON. Rare language metadata may be JSON. Required focused tests include:

| Test family | Evidence required |
| --- | --- |
| Successful replacement | Staged TU facts replace the prior original-file/symbol contributions in one transaction; generation increments only with successful commit |
| Failed analysis and rollback | Prior committed facts/hash/generation remain unchanged; failure/stale attempt state is separate; injected SQL failure between removal and insertion leaves no half-generation |
| Crash and writer exclusion | A controlled child writer process dies before commit; reopening yields the old intact generation; an independent writer cannot acquire the OS lock; process exit releases it without PID-file deletion |
| Reader snapshot | A reader sees one committed generation while another process commits; never a mixture of old locations and new evidence |
| Multi-TU evidence | Replacing/removing one TU preserves the other TU's header locations, relation evidence and unresolved owners; final contribution removal cleans unreferenced nodes |
| Compile-context replacement | Define/include/toolchain or selected-command changes reconcile old contributions rather than append facts under a new command fingerprint and leave old evidence live |
| Fingerprint separation | File bytes, declarations/API, symbol bodies, retrieval documents, deep inputs and compile actions have independent fingerprints; ranking failure does not invalidate successful structural facts |
| Dependencies versus output scope | Ignored/generated/external inputs remain in dependency invalidation metadata but not public node/search/source-body results; deletion and new ignore immediately suppress public stale results |
| Bounded freshness | Recheck after writer-lock acquisition; large fan-out returns stale with explicit-build guidance within the PRD budget; status-only checks do not invoke project build, UBT/UHT or deep |
| Worktree isolation | Separate mutable databases/locks; seeded facts are revalidated against the current worktree; incompatible snapshots are rejected rather than assumed fresh |

WAL is preferred only where supported; rollback-journal fallback is required. The PRD's complete mutation matrix (including Unreal context changes) remains the ultimate Phase 2 acceptance, not just the ordinary-C++ subset above.

### Remaining Phase 1 contracts beyond the bounded analyzer increment

The coordinator checked the Phase 1A design against FR-GPH-001..005 and FR-CPP-001..005. The following are required before ordinary-C++ Phase 1 acceptance; they are not an instruction to interrupt or enlarge the current bounded assignment. A passing four-case matrix does not imply these are implemented.

| Contract | Further evidence required |
| --- | --- |
| Template and alias completeness | Class/function primary and explicit/partial specialization, explicit instantiation distinction, implicit-use arguments without public-node explosion; alias spelling plus resolved target |
| Include observations | Original direct-include location, spelling, resolved target and condition; repeated/header-only inclusion contexts; no transitive stored edges or PCH/external ranking signal |
| Inheritance metadata | Direct base access, virtual flag and declaration order; no transitive stored inheritance or interface heuristics |
| Callable completeness | User constructors/destructors/conversion/operators, defaulted/deleted states, referenced hidden implicit special members, copy/move and base/member initializers; automatic destruction marked implicit lifetime; no built-in callable nodes |
| Lambda and indirect boundaries | Value/reference and explicit/implicit captures, this/star-this/init-capture, nested lambdas; address references; provable immediate indirect invocation versus unresolved pointer/container calls |
| Type references | Raw pointer and nested standard smart-pointer/container type references with wrapper spelling; no invented ownership or inheritance |
| Physical/semantic boundary | Original header/cpp/inl locations and inclusion contexts, external referenced-only placeholders, no external body/internal-call/path search output; actual case-sensitive root-containment and Unicode path tests |
| Missing/limited compile context | Header-only consumer context, ambiguity, source absent from compilation database and no-context fallback; unsupported options and response inputs remain visible limitations until supported, never silently verified |

All Unreal contracts remain separate: real UBT/UHT provenance and original-source mapping, generated/unity/PCH filtering, reflection/RPC/interface/delegate relations and resolved module/target facts require the selected real engine profile. Ordinary-C++ success cannot substitute for that evidence.

Benchmark preparation must freeze profile/scope and expected symbols/relations before evaluation. Failed or unresolved in-scope facts stay in the recall denominator. Public retrieval development/holdout manifests must be stratified by query type and group same-intent paraphrases together before tuning; no release-quality claim is based on implementation fixtures alone.

### Local verification host

Read-only Windows inventory on 2026-09-18 reports Windows 11 Pro 10.0.26200, Intel Core i7-13700KF (16 physical / 24 logical cores), 127.79 GiB physical RAM and a Samsung SSD 980 PRO 2TB NVMe containing the D: workspace. This meets the PRD's stated hardware floor for later local performance measurements. No query/index performance measurement has been run, filesystem cache state is not controlled, and this machine is not evidence of either a clean no-Unreal runner or the required selected Unreal runner. Each eventual benchmark must record its own input, toolchain, cache state and complete-versus-partial output accounting.

### Independent invocation-safety fixture matrix

Agent3 prepared the following eight cases without running dangerous inputs or inspecting the in-progress analyzer. These are acceptance tests to implement/review, not evidence of passing behavior. Here, accept means allow the safe invocation; it does not guarantee the source compiles successfully.

| Input case | Expected boundary |
| --- | --- |
| `sccache.exe clang-cl.exe /c src/a.cpp` | Reject unsupported wrapper; never execute it or guess a driver by shifting argv |
| `clang-cl /c /clang:-Xclang /clang:-load /clang:C:/plugins/x.dll src/a.cpp` | Reject before frontend; never load plugin code or forward the wrapper as unknown |
| `clang++ -I -DNAME -I src/a.cpp src/a.cpp` | Preserve the two include operands literally; only final a.cpp is source, never reinterpret -DNAME as a define |
| Windows command with include `C:/SDK & Tools/inc(1)` and source `C:/Repo & 100%/src/한 글#x.cpp` | Quoted token boundaries and Unicode survive; no shell, environment expansion or punctuation splitting |
| Individual `@opts.rsp`, `--config=cfg`, `-ivfsoverlay map.yaml`, `-fmodules-cache-path=cache` options | Reject unsupported semantic injection/cache writes before frontend; do not apply hidden contents |
| `clang++ -c src/a.cpp -o obj/a.o -MD -MF obj/a.d` | Invoke only after explicit audited removal of output/dependency options and operands; no object/dependency output |
| `clang++ -c src/a.cpp src/b.cpp`, entry a.cpp | Reject additional source input; no silent first-source selection or combined unit |
| `clang++ -D src/a.cpp`, entry a.cpp | Reject missing actual source after operand consumption; never append it and invent context |

All rejected units retain raw input and actionable reason, never reach ToolInvocation, and cannot mutate prior accepted facts. Ordinary source/header/forced-include/resource/external dependency reads are allowed within the input policy; that does not authorize compiler wrappers, plugins, config/VFS overrides or output/cache side effects.

Coordinator policy resolution: unusable always rejects; degraded rejects by default. The bounded implementation may allow only explicitly understood safe degradation while retaining its status and limitations. It need not implement recovery for every reason. Missing source, unexpanded semantic input, unknown passthrough and additional actual sources always reject. Safe compiler diagnostics may yield a staged completed-with-errors result, but that is neither safety acceptance nor automatic publication of replacement facts.

### Public C++ corpus preparation

The coordinator downloaded the fixed `{fmt}` commit ZIP to ignored `build/deps/downloads/fmt-1be298e1bd68957e4cd352e1f676f00e07dcfb57.zip` from the official GitHub codeload endpoint. The 804,255-byte archive has computed SHA-256 `107eb39fd3cc0df8f0068451e2f08a530f1418feabc6badb7202e03765ab64d3`; its `include/fmt/base.h` contains `FMT_VERSION 120200`. This is a locally computed download pin, not an upstream signed checksum. No extraction, corpus configuration/build or semantic measurement has occurred.

The pinned [upstream CMake configuration](https://raw.githubusercontent.com/fmtlib/fmt/1be298e1bd68957e4cd352e1f676f00e07dcfb57/CMakeLists.txt) can enable modules automatically for supported C++20 toolchains. The proposed initial non-module consumer profile should therefore explicitly set `FMT_MODULE=OFF`, `FMT_TEST=ON`, `FMT_DOC=OFF` and `FMT_INSTALL=OFF`, retain Unicode, and record compiler/standard/OS-API settings. This is an explicit evaluation scope, not module-support evidence or permission to drop in-scope failed facts. The pinned [test configuration](https://raw.githubusercontent.com/fmtlib/fmt/1be298e1bd68957e4cd352e1f676f00e07dcfb57/test/CMakeLists.txt) includes header-only and Unicode consumer targets. Build/profile generation and exact TU/gold-set selection are still pending. Neither source preparation nor upstream tests establish LiveCodeMap precision, recall or retrieval quality.

Subsequent corpus build evidence: the archive hash was rechecked, all 167 entries were checked for containment, and the source was extracted without modifying upstream files into `build/corpora/fmt-1be298e1bd68957e4cd352e1f676f00e07dcfb57`. Configuration/build succeeded in the separate `build/corpora/fmt-clang22-cxx20-release` directory using the pinned clang-cl 22.1.0, VS 2022 Professional x64 environment, MSVC STL 14.44.35207, Windows SDK 10.0.26100.0, CMake 3.30 and VS-bundled Ninja. Release/C++20, the options above, `FMT_OS=ON`, `FMT_FUZZ=OFF`, `FMT_CUDA_TEST=OFF` and `CMAKE_EXPORT_COMPILE_COMMANDS=ON` were explicit. The compiler environment was initialized through the installed `Microsoft.VisualStudio.DevShell.dll` / `Enter-VsDevShell`; no global environment or locale setting changed.

The actual compilation database contains 51 entries / 31 distinct files: 50 C++ entries and one C entry, including 21 header-only command variants and the Unicode test consumer. Its local byte SHA-256 is `cd929133df73657ced29cc2c92a546f58186eca1819bedabcb298e253a0db82b` (includes machine-local absolute paths, not a portable semantic fingerprint). Full upstream build finished all 77 Ninja steps successfully. For later LiveCodeMap evaluation, explicitly select C++ command identities and freeze intended scope; do not blindly merge alternatives or silently include unsupported C as C++.

Upstream CTest is **19/21 passing**, not fully passing: `chrono_test.locale` throws `failed to format time`; `unicode_test.legacy_locale` returns unexpected encoded weekday text. Both repeat outside the sandbox. This host's user/system locale is ko-KR, but the precise upstream/CRT cause is not proven. Full log: `build/reports/fmt-upstream-ctest.log`. These failures were not skipped, patched, or hidden, and machine locale was not changed. The corpus can now supply real compilation contexts; no LiveCodeMap analysis/graph/retrieval measurement of it has occurred and no release gate is thereby met.

The coordinator saved `build/reports/fmt-input-snapshot.json` with all 51 original command strings/directories, repository-relative source paths and their actual SHA-256 hashes, plus the explicit profile and upstream test limitations. The 50 C++ entries are marked candidates and the single C entry is explicitly outside this initial C++ profile. This is an ignored, machine-local input snapshot, not a selected gold set, finalized compile-command identity list, portable benchmark manifest, or passing product evaluation. In-scope failures must remain visible when the later evaluation scope and gold facts are frozen.

### Phase 1A independent verification in progress

At the 13:30 handoff (`build/reports/phase1a-handoff.md`), the coordinator independently rebuilt Release and ran the full CTest suite: 5/5 passing (core, Clang smoke, analyzer, SDK bootstrap, SDK discovery). Direct executable reruns confirm core 81/1,317 and analyzer 11/402 in Release, Debug and RelWithDebInfo; smoke is 8/67 in the refreshed configurations. Missing-include/broken-source diagnostics in the analyzer suite and the smoke diagnostic are intentional negative cases, not failing tests. The worker additionally reports the separate Clang-disabled Release build/CTest 3/3; this remains worker evidence until independently checked.

Agent3 is auditing actual analyzer source, IR changes, fixture coverage and safe isolated negative/cross-composed reproductions under `build/reviewer-phase1a`. Existing passing fixtures do not establish acceptance by themselves. Implementation source is read-only during the Claude usage-limit wait; if its automatic 14:00 resume changes source, the reviewer must account for those changes before issuing final findings. No model replacement, additional agent or cancellation of that automatic wait has been requested.

The worker's temporary core-only build was already cleaned up when independently rechecked. The coordinator therefore configured a new `build/coordinator-coreonly` tree with `LCM_WITH_CLANG=OFF`, reusing only local dependency sources whose hashes were checked against `Dependencies.cmake`. Fresh MSVC Release compilation and its core CTest passed; no Clang semantic target is built in this configuration. This verifies optional build separation, not semantic analysis without Clang. Initial fresh-toolchain detection was denied inside the sandbox; the same configure/build succeeded with the permitted toolchain-access escalation, without changing project source or global compiler settings.

Coordinator source-level finding pending isolated runtime reproduction: `analyzer.cpp:167-168` wraps `getRealFileSystem()` in an overlay and sets its working directory. The pinned SDK header documents this filesystem as process-CWD-linked, unlike `createPhysicalFileSystem()`. The [pinned LLVM implementation](https://raw.githubusercontent.com/llvm/llvm-project/llvmorg-22.1.0/llvm/lib/Support/VirtualFileSystem.cpp) confirms overlay delegation and a process current-path write for the linked instance (constructor 277, setter 377, factories 422/430, overlay setter 525). No restoration exists in the analyzer function. Agent3 was asked to assert process current_path before/after an isolated safe analysis; a per-unit independent physical filesystem is the likely minimal correction. This observation is not a product-source change or an acceptance result.

Coordinator runtime path-boundary findings: an isolated diagnostic links the actual current Release core library (`build/coordinator-boundary-check/probe.cpp`, its CMake project and executable), without modifying product source. On a freshly created case-sensitive Windows temp fixture it confirms distinct `CaseRoot`/`caseroot` directories and distinct `a.cpp`/`A.cpp` files. `make_repo_relative_on_disk(CaseRoot, caseroot/external.hpp)` nevertheless returns `external.hpp` as in-root; `commands_for_file(a.cpp)` returns both distinct commands and `select_unique` reports ambiguity. The source paths are `source.cpp:88` (canonical paths still use an ASCII-folded prefix) and `compile_db.cpp:300` (lexical equality wins even when existing files are distinct). These are not the already corrected normalizer comparison function. Required regression outcomes are an external boundary for the sibling and exactly the selected file's command, while preserving ordinary case-insensitive/Unicode aliases.

The workspace-volume fixture could not enable case sensitivity (Win32 error 5); the successful reproduction used only a newly created system-temp test directory, as the existing foundation regression does. No system-wide case sensitivity or locale setting was changed. The diagnostic files/build are ignored artifacts, not production implementation. Agent3 received the observed results and exact repro command for independent audit consolidation.

### Phase 1A audit correction assignment (prepared for implementation resume)

Agent3 completed its audit at 13:48 and withheld acceptance. The coordinator reran both `build/reviewer-phase1a/out-vs/Release/phase1a_probe.exe` and `build/reviewer-phase1a/gate_probe.exe`, confirming the reported outputs. Those diagnostics use safe macro inputs or gate-only checks; dangerous plugin/config options were not executed. Implement the following within agent2 ownership, then freeze source and request re-review. Do not advance to storage or broaden the current feature increment before correcting these defects.

| Priority / finding | Required correction and regression evidence |
| --- | --- |
| High: process CWD changes (`analyzer.cpp:167`) | Independent per-TU filesystem working directory, not a process-linked singleton or global chdir/restore workaround. Host CWD stays unchanged for successful/error/rejected calls and for sequential units with different directories; include resolution still uses each selected directory |
| High: command/context provenance mismatch (`analyzer.cpp:108`, request API) | Cannot combine a safe normalized context with a different raw command and publish verified facts under that command. Internally normalize the actual command, or validate a complete binding including argv/source/directory/identity. Test mismatched defines/source/command and unsafe raw options against a safe context; they must not silently pass with false provenance |
| High: external sibling accepted as in-root (`source.cpp:88`) | Existing path containment must use filesystem-authoritative ancestry, not ASCII-folded canonical prefix. CaseRoot/caseroot sibling remains external; preserve ordinary Unicode/case aliases, nested descendants and symlink boundary behavior |
| High: template primary IDs collide (`facts_builder.cpp:506/553/675`) | Distinguish `template<class T> int collision()` from `template<int N> int collision()` with canonical template-parameter signatures on symbols and relevant owner components. Both primary nodes and both compiler-resolved call targets must differ. Parameter renaming/redeclarations must not create new IDs; keep non-template identity golds unchanged |
| High: broad option-prefix bypass (`safety.cpp:117`) | Replace blanket -m acceptance and overly broad -f/MSVC option prefixes with understood option forms. Gate-only reject `-multi-lib-config=cfg.yaml`, `-munknown-lcm-option`, `-fexceptions-unknown` and `/OUT:...`; ordinary supported option positive controls still pass. No dangerous config/plugin option is invoked for a test |
| Medium: case-distinct command selection (`compile_db.cpp:300`) | Existing a.cpp and A.cpp resolve to their own command only; filesystem equality precedes lexical fallback. Preserve aliases of one actual file and the legitimate multiple-profile ambiguity case |
| Medium: anonymous namespace-scope types omitted | Two anonymous namespace-scope structs produce distinct stable identities using meaningful enclosing/file anchor and ordinal consistent with the identity contract. Preserve local anonymous types; do not silently drop a classified supported declaration or use compiler-generated absolute path/line spelling |
| Medium: call metadata aspects overwrite one another (`facts_builder.cpp:884`) | Preserve evaluation context, dispatch and template-use aspects independently. Default-argument virtual call retains both default evaluation and selected virtual slot; init-capture implicit-template call retains both capture evaluation and template folding/arguments. Include evidence equality/hash and immediate-lambda metadata regressions |
| Low: same-class qualification mislabeled as base (`facts_builder.cpp:257`) | `d.Derived::run()` must not claim a base-qualified call. A general qualified/devirtualized classification is acceptable; retain confirmed exact targets and explicit Base::run behavior |

Coordinator API clarification for provenance: an explicit logical contribution-unit label is not inherently forbidden. It must not replace or falsify the selected compile-command provenance. Preserve a justified custom unit label only with its actual command/context identity retained; do not invent a PRD requirement that every logical unit string must equal the command hash. Any schema/anchor choice needed for anonymous global types must be documented explicitly and reviewed, not hidden behind a false supported claim.

The coordinator independently observed `cwd_changed=1`, mismatched input accepted as `unsafe-unit`, `collision_symbols=1`, `anonymous_types=0`, lost combined call roles, and all four invalid gate examples accepted. CL/CCC_OVERRIDE_OPTIONS macro probes produced no injected symbol in this ToolInvocation path, and the referenced external inline declaration did not expose its internal call; those negative findings are not reported as defects. The separate two filesystem findings retain their actual Windows temp-fixture evidence. Full-PRD gaps remain separate from these nine correctness/safety corrections.

The native Claude session resumed shortly after 14:00, finished its interrupted documentation/report bookkeeping, and then received the correction assignment through Herdr. The coordinator did not answer a dialog, restart the session, cancel its rate-limit wait or change the requested model. A coordinator-held SHA-256 snapshot of 64 source/test/build-support/documentation files records the pre-correction audit state for comparison; this is not a release integrity manifest.

Post-correction verification must **rebuild/relink** isolated diagnostic executables against the corrected static libraries before interpreting their output. Their old binaries embed the pre-correction code. If the analyzer request API eliminates externally supplied normalized contexts, agent3 should adapt its isolated probe to that API and verify unsafe actual raw commands reject; an old probe failing to compile against the deliberately removed field is not itself a product regression. Agent2 must leave reviewer/coordinator diagnostics untouched and prove the corrected contracts through product regression tests as well.

In-progress follow-up to finding 6: the coordinator rebuilt its boundary diagnostic against the changed Release core at 14:16. Filesystem containment now reports the distinct sibling as external, and lookup returns exactly one case-distinct file command. However, two actual case-sensitive sibling directories `CaseRoot`/`caseroot`, each with a different `a.cpp` and identical relative arguments `cl.exe /c a.cpp`, still receive the same computed command ID. The diagnostic reports `different_directory_sources=1 equal_raw_arguments=1 command_id_collision=1`. Agent2 received this related provenance/selection correction before handoff: existing distinct paths must not collapse through ASCII case-folding in command identity. Add a real-directory regression and retain same-file canonical alias behavior where supported. These are intermediate observations, not acceptance of the unfinished corrections.

After that follow-up implementation, the coordinator rebuilt/relinked the isolated boundary executable again against the 14:24 Release core. The executable now exits 0 and reports actual distinct directories/files, `external_sibling_reported_in_root=0`, `lower_file_candidate_count=1`, and `command_id_collision=0` with identical raw arguments. This independently verifies these three reproduced boundaries only. Full product suites and the nine-finding semantic/safety re-review are still pending. Agent3 is preparing its isolated probe for the intentionally changed request/evidence API without building or editing the shared product tree during agent2's remaining builds.

The final correction handoff is `build/reports/phase1a-corrections.md` (14:29). Agent3 received explicit authorization to rebuild its isolated probes and re-review the frozen product source at 14:30. The coordinator independently reran all nine core/analyzer/smoke executables across Release, Debug and RelWithDebInfo: each configuration passes core 86/1,367, analyzer 14/578 and smoke 8/67 with zero skips. It then rebuilt the separate `build/coordinator-coreonly` tree from the corrected source; CMake confirms Clang targets absent and its core CTest passes. The case-sensitive test helper/CMake changes and anonymous-type declarator/record anchor choice are explicitly included in the independent audit. No next feature assignment is authorized before that review outcome.

During that re-review the coordinator independently ran the freshly relinked reviewer analyzer probe: all 30 explicit semantic/provenance/CWD checks pass. The gate probe initially had an incorrect expectation that normalization itself removed `/OUT:`; the documented contract keeps it as `kept_unknown` for the independent gate to reject before frontend invocation. Agent3 corrected only its scratch assertion, not product code, and retained the actual safety-rejection checks. The product audit remains open, including paths/identity and anonymous-type anchor edge cases. `build/reports/phase1a-frozen-review-input.json` records SHA-256 for 68 source/test/build-support/docs/tool files as an audit input snapshot, not a release manifest.

Agent3's 14:45 outcome closes the nine original direct regressions and accepts the documented anonymous-type anchors within this increment. Its expanded probes expose two follow-ups, independently rerun by the coordinator:

- Medium: `compute_command_id` still folds output path case. Real distinct `out.obj`/`Out.obj` in a case-sensitive temp directory share an ID; `select_unique` can therefore mistake two output profiles for one. Fix and test identity distinction plus creation/deletion stability. Coordinator chooses a normalized case-preserving **lexical** output identity, independent of artifact existence; case-insensitive output aliases may conservatively remain separate command identities and ambiguity. Document and test that policy, while keeping the corrected existing-file/directory identity behavior. Do not apply the current `fs:`/`lex:` existence-sensitive helper to output artifacts.
- Low: explicit non-virtual `p.Plain::run()` receives `static_target`, contradicting the public generic `qualified` contract. Move qualifier handling outside the virtual-only branch; assert qualified non-virtual and unqualified non-virtual positive controls while preserving virtual/final/base cases.

The implementation freeze is lifted only for these fixes and directly related regressions/docs. Agent2 must finish its current isolated feasibility observations, then prioritize the two corrections and return a new handoff. Reviewer/coordinator scratch files stay outside its ownership. No next product feature is authorized yet. Agent3 confirmed product source/test hashes were unchanged during its audit.

Final Phase 1A acceptance at 14:59: agent3 found no remaining actionable defect in the bounded two-fix review. Fresh isolated probes pass analyzer 32/32 and gate/command identity 11/11, including output creation/deletion and ambiguity plus nonvirtual qualified/unqualified controls. The coordinator independently reran those probes and all three product configurations (87/1,388 core, 14/587 analyzer, 8/67 smoke), and rebuilt its separate Clang-disabled core successfully. A transient model-capacity error interrupted the reviewer's final response after successful test execution; one retry in the same requested gpt-5.6-sol high session completed the verdict. No model/session replacement occurred. The freeze is now released for the explicit Phase 1B contract; full Phase 1 and release gates remain open.

### Six further ordinary-C++ fixture contracts (preparation, not current assignment)

Agent3 derived these from FR-GPH-003/004 and FR-CPP-001/003/004/005 while the nine corrections were being implemented. No current product behavior was accepted or changed by this exercise; the fixtures must first be compiled and their compiler evidence checked during the later assignment. These are representative tests, not a substitute for complete Phase 1/first-release coverage.

| Fixture family | Positive and negative assertions |
| --- | --- |
| Defaulted/deleted and implicit members | Explicit Policy default/move constructors and destructor retain defaulted state; deleted copy constructor retains deleted state with no call edge. A copied Plain object has compiler-referenced hidden implicit copy/destructor identities, with construction and implicit-lifetime evidence; unreferenced implicit members do not become public search entries. Prefer a member with a declared nontrivial destructor to make implicit destruction evidence unambiguous rather than inventing trivial-codegen calls |
| Initializers and lifetime | Box derives Base, contains Part, and initializes Base(int) then Part(Part&&): calls belong to Box's constructor, with base/member identity. Caller constructing Box and copying Part has distinct constructor targets, explicit conversion and user operator+ calls; its automatic destruction targets are marked implicit lifetime. Built-in addition creates no callable; subobject initialization is not flattened into the outer caller |
| Captures and immediate indirect calls | Three Worker::run lambdas cover explicit value/reference/this/init captures, implicit reference/this, and star-this capture. Init-capture make_value belongs to run; body target calls belong only to lambdas. `(*&target)(3)` is directly provable; `fp(4)` stays unresolved. No general def-use expansion or inferred pointer-variable target |
| Alias and wrappers | SharedNode=shared_ptr<Node>, NodeList=vector<unique_ptr<Node>>, and raw/shared/weak/nested fields retain alias source spelling, resolved types and wrapper nesting. Fields and alias definitions reference Node. No invented owns/extends/calls, and no public explosion of external standard-library implicit instances |
| Conditional includes and fragments | Two real consumer TUs include api.hpp with and without USE_FAST. Active direct includes carry raw condition/branch evidence, spelling, target and original span/hash; one common() in detail.inl retains both contributions. FastOnly/SlowOnly stay attached to their actual contexts. No stored transitive include edges, invented standalone header context or merged define state |
| Template forms | Primary Box<T>, partial Box<T*>, explicit specialization Box<int>, and explicit instantiation Box<long> are distinguishable. Implicit Box<double> and Box<char*> uses fold to the actually selected primary/partial method pattern with concrete arguments, without public implicit nodes. Explicit specialization/instantiation uses have their actual distinct targets; primary parameter signatures prevent collisions |

Coordinator representation direction for the later increment:

- Preserve declaration-specific metadata with its contributed location/evidence; derive logical display state without last-writer-wins loss of per-TU provenance.
- Preserve destructor ownership through the destructor/subobject boundary rather than flattening all subobject destruction into the outer caller. The PRD does not require a new general execution-order/dataflow model merely to assert this fixture.
- A capture of a non-public local may use its name plus hashed declaration location/descriptor; do not introduce public local-variable indexing solely for capture metadata.
- Keep raw alias/wrapper spelling and resolved semantic representation separately. Use the smallest representation preserving nesting; a new general-purpose type-AST storage framework is not a prerequisite.
- Keep active semantic include evidence separate from any inactive structural observations. Raw condition stack, branch kind and evaluated status are sufficient; do not invent confirmed inactive dependencies from textual guesses.
- Prefer one explicit-instantiation logical role with declaration/definition locations, not separate identities for its declaration and definition. Implicit folding must retain the selected pattern and arguments. Add cross-TU declaration/definition and parameter-renaming cases before claiming broad template correctness.

These directions are scoped engineering decisions to be checked against actual compiler evidence and the full PRD during implementation. They do not authorize advancing beyond the outstanding nine corrections.

### Next increment preparation: callable and lambda completeness

While agent3 re-reviews the frozen corrections, agent2 has a read-only feasibility assignment against FR-CPP-003/004 and the pinned Clang headers. It may propose compiler APIs and fixture contracts, but may not change implementation, tests or documentation or build the shared tree. This planning work is not approval of Phase 1A or authorization to implement the next increment.

The proposed next implementation boundary is the first three fixture families above: callable declaration state and referenced hidden implicit members; compiler-confirmed construction/copy/move/conversion/operator and initializer/lifetime evidence; captures and immediately provable indirect calls. Full type-wrapper/alias representation, conditional includes, remaining template forms, no-context behavior, Unreal and every later phase stay required rather than being redefined away.

Additional acceptance directions for that proposal:

- Declaration-specific defaulted/deleted/implicit metadata must survive multiple TUs and contribution removal; source edits must not leave stale last-writer-wins symbol flags. Keep metadata out of stable-ID identity unless it changes the canonical signature/linkage itself.
- Do not manufacture a body/source definition for an implicit declaration. Preserve compiler-derived origin and a truthful source anchor; referenced hidden members are not ordinary public indexed declarations or external placeholders.
- Derive automatic-object destruction from actual compiler evidence, including nested scopes and early returns. Keep implicit lifetime distinct from an explicitly written destructor call. No general execution-order graph or target inference from variable flow is required.
- Capture metadata must preserve target descriptor, value/reference, explicit/implicit, this/star-this and init-capture without turning all locals into public symbols. Keep initializer/body ownership distinct, including nested lambda cases.
- Verify nontrivial member destruction, deleted-member negative cases, unreferenced implicit suppression, no built-in operator nodes, unresolved pointer-variable calls, original-byte locations, deterministic evidence hashes, and existing regression suites in supported configurations.

The coordinator will resolve any representation ambiguity and assign the actual bounded implementation only after the correction audit outcome. Direct-base access/virtual/order metadata remains an explicit Phase 1 requirement even if not included in this next increment.

Read-only feasibility report: `build/reports/callable-feasibility.md` records pinned-header inspection and scratch AST/clang-query observations. Its first proposal is not accepted as implementation authorization. Coordinator follow-up decisions: preserve defaulted and deleted state orthogonally (an explicitly defaulted special member may also be implicitly deleted); keep hidden-destructor ownership for subobject lifetime; do not conflate closure destruction with execution of the lambda body (only closure `operator()` is explicitly folded by the PRD); distinguish ordinary capture copy from init-capture evaluation; do not use an `implicit_call` flag to stand in for optional elision. Parameter lifetime evidence must not be fabricated for a declaration without a definition. Agent2 is checking the AST-only lifetime proposal against actual pinned Clang CFG evidence for return/elision, arrays, nested/early exits and parameters. Using compiler CFG to extract truthful destructor evidence does not require storing a general execution-order or path graph. No new product implementation is authorized by this follow-up.

The follow-up `build/reports/callable-feasibility-cfg.md` proposes extracting distinct implicit-destructor elements from compiler CFG without retaining blocks/edges/order. It records scratch observations for arrays, early exits, NRVO, prvalue elision, discarded temporaries and defaulted-but-deleted members; these are feasibility observations, not passing product features. Its parameter ownership rule is **not accepted**: the coordinator's pinned-driver `-###` observations show both default `cl` and `g++` modes target `x86_64-pc-windows-msvc` (cc1 triple includes MSVC 19.44.35227). Driver syntax alone therefore cannot choose MS versus Itanium ABI. Availability of a callee body also must not arbitrarily change a real call's ownership. Agent3 has a bounded read-only review of these parameter/CFG assumptions while the two current product defects are fixed. Any implicit-used destructor CFG extraction needs actual evidence before a record-type fallback may claim confirmed subobject calls.

Agent3's 14:53 design review and direct pinned-API probe (`build/reviewer-callable`) establish the acceptance direction: both default Windows driver modes report `isParamDestroyedInCallee()==true`; an explicitly selected `x86_64-unknown-linux-gnu` target reports false. Definition availability does not change that ownership. A caller-side `CFGTemporaryDtor` is ABI-agnostic and cannot blindly become a caller-owned confirmed destructor for a callee-destroyed argument. If the correct external/declaration-only boundary cannot be represented, retain an explicit limitation rather than relabeling the owner. The same probe successfully builds the used implicit destructor's synthesized-body CFG and obtains one member and one base destructor in each of the three target/mode configurations. Consequently, the next increment should use real CFG elements for these confirmed calls; missing body/CFG must produce an explicit limitation, not a record-field fallback promoted to confirmed compiler evidence. Tests must cover default Windows modes, an explicit alternate target, declarations versus definitions, and absent-CFG negative behavior. This is compiler feasibility evidence, not completed product behavior.

### Phase 1B implementation contract (activated after 14:59 Phase 1A acceptance)

Owner: agent2 for analyzer, minimal shared-IR/build changes, fixtures/tests and implementation docs. Agent1 resolves scope/representation; agent3 independently reviews the handoff. Output: real compiler-backed callable/lambda facts integrated into `FactSet`, not a parallel smoke-only model. This increment does not replace the remaining ordinary-C++, Unreal, storage or release requirements.

1. Preserve user-declared constructors/destructors/conversions/operators and orthogonal contributed defaulted/deleted state, including explicitly defaulted but implicitly deleted members and out-of-line definitions. Retain compiler-referenced hidden implicit special members without making unused implicit members public. Hidden metadata must carry contribution ownership even if it has no user-written declaration span; mixed contributions and removal must not leave last-writer-wins flags or orphan surviving endpoints. Stable IDs must not depend on the new display/state metadata.
2. Record compiler-resolved construction/copy/move/conversion/user-operator calls and written/implicit base/member/delegating initializer calls with correct callable ownership. Preserve evaluation, subobject and template/dispatch aspects independently. Do not manufacture built-in operator callables. General-purpose extra call flags should be added only with a precise compiler-backed meaning and a concrete requirement/test.
3. Extract implicit destruction from actual compiler CFG elements, deduplicated by the meaningful object/subobject site rather than CFG path count; retain lifetime character, truthful source anchor and potential-elision information. Cover local arrays, nested scopes/early exits, discarded temporaries versus guaranteed prvalue elision, and used implicit destructor member/base elements. Do not persist a general CFG/order graph. Missing body/CFG is a visible limitation, never confirmed record-field guesswork.
4. Use the compiler's actual parameter-destruction ABI decision, not driver syntax or definition availability. Suppress falsely caller-owned argument destructor facts in callee-destroyed contexts. A declaration alone is not a fabricated body; a real callsite plus compiler parameter evidence may support an in-root callee-owned observation with truthful provenance. External/declaration-only cases that cannot be represented safely stay explicit boundaries, not reassigned owners or indexed external implementation bodies. Verify both default Windows driver modes and an explicit alternate target with header-free fixtures.
5. Keep closure destruction distinct from lambda body execution: a compiler-used closure destructor may have a hidden local identity anchored to the lambda and own its capture subobject destruction. Only `operator()` folds into the callable lambda. Preserve captures (target descriptor, value/reference, explicit/implicit, this/star-this, init-capture) without public-local indexing or def-use expansion. Ordinary capture copying and init-capture evaluation remain distinct. Nested lambdas, function/method address references and directly provable `(*&target)(...)` must retain exact attribution; pointer-variable/container callback targets remain unresolved rather than guessed.
6. Every new fact participates in equality, deterministic fact hashing, publication and contribution removal. Capture/hidden-symbol observations must preserve endpoint liveness when another contribution is removed. Verify three-run determinism, stable existing named IDs, original source hashes/spans and external referenced-only boundaries.

Verification: implement the prepared first three fixture families plus the concrete compiler counterexamples above; run affected core/analyzer/smoke suites in Debug, Release and RelWithDebInfo and the core-only build. Report exact test results, supported/limited cases and changed files; freeze for independent review. Keep type-wrapper/alias completeness, full template/include forms, direct-base metadata, allocation/deallocation operator coverage where still missing, no-context behavior, UE and all later release gates explicitly pending rather than declaring all C++ or Phase 1 complete. No provider spending, public release, commits or extra agents are authorized by this bounded assignment.

Herdr dispatch at 15:00: agent2 received and started this Phase 1B implementation. In parallel, agent3 received a bounded read-only survey of the pinned `{fmt}` compilation database through the accepted normalizer/safety gate, without frontend analysis or executing database commands. It may use only its isolated `build/reviewer-fmt-context` diagnostics and must preserve alternative profiles and the explicit C++/C scope. Requested output is actual normalization/gate counts and unique blockers for later adapter planning, not a gold set or quality measurement. Shared product builds remain agent2-owned while implementation is active.

Early coordinator code-review risk for Phase 1B, pending regression execution: nontrivial by-value arguments must compose correctly with lambda calls, member operator calls, template folding and unresolved function pointers. The draft calls its ABI ownership helper after folding a concrete callee to its dependent template pattern, returns before that helper for lambda targets, and does not explicitly offset the implicit object in member-operator argument mapping. Agent2 received focused regression requests for all four forms, including suppression of falsely caller-owned temporary destruction when the ABI destroys in the callee but the callee target is unresolved. These are source-level risks to test/fix during implementation, not claimed completed runtime findings or a demand for general dataflow.

The subsequent draft moves ABI handling before target folding/early returns and accounts for the member-operator object argument. Verification must include a genuinely dependent destructible parameter (`template<class T> void dependent_take(T)` instantiated with a nontrivial record); a `Tracker` parameter beside an unrelated `T` parameter alone cannot expose the original folding risk. Both actual MSVC-target callee ownership and explicit Itanium-target caller ownership remain required.

Coordinator IR review also found a mixed-contribution presence risk: after hidden H and user-declared D locations share an identity, removing D leaves nonempty hidden locations but the draft retains `indexed`. Agent2 is to recompute visibility from surviving location roles and test both insertion/removal orders, endpoint-only liveness, and hash equivalence with a freshly reconstructed surviving set. Agent3 has a separately scoped isolated snapshot reproduction while shared product builds remain agent2-owned. This in-progress finding is not acceptance or a whole Phase 1B review.

That focused IR defect was reproduced and then closed in isolated copied-source executables (`build/reviewer-phase1b-ir`), independently rerun by the coordinator. The original `facts.cpp` SHA-256 `850f66d3fe8ef3b0f6cf74e56f63e433471976285d332ab4e74f7f0110e4e5ed` keeps `indexed` after removing D for both insertion orders and differs from fresh H-only hashing. The corrected source at 15:22:57, SHA-256 `44638b4c8d50e88b0aa4f29e2bf525c8d5fe14ad1872dc2783abefb0178a895c`, reports `implicit_hidden` and equal survivor hashes in both orders. Removing H preserves indexed D and its fresh hash; surviving relation/capture endpoints remain `endpoint_only`. No shared product build or product edits were made by the reviewer/coordinator. This closes only the focused mixed-presence finding; Phase 1B implementation and full review continue.

Phase 1B handoff: `build/reports/phase1b-handoff.md` at 15:32 freezes the source for review. Agent2 reports full CTest 5/5 in each of Debug, Release and RelWithDebInfo, plus separate Clang-disabled CTest 3/3. The coordinator independently reran all nine core/analyzer/smoke executables: every configuration passes 91/1,487, 19/1,351 and 8/67 respectively, with no skips. It also rebuilt its separate `build/coordinator-coreonly` tree and ran the core test successfully. The first sandboxed MSBuild attempt hit the previously observed host `PATH`/`Path` duplicate-key error; the identical build with normal toolchain access succeeded. This is environment evidence, not a product defect or a waived failing test. `build/reports/phase1b-frozen-review-input.json` records 70 product/test/build-support/docs file hashes as audit inputs, not a release manifest.

Agent3's full bounded audit begins against that handoff, including unresolved member-function pointer argument lifetime in addition to the implemented free-pointer cases, and contract review of documented closure-copy/unnamed-constructor/virtual-base limitations. Product and shared build remain frozen. Agent2 may only read existing normalizer/gate code and propose the next adapter changes/tests; no implementation or new shared builds are authorized before the audit outcome.

Phase 1B audit outcome at 15:44: acceptance is held for one high-severity confirmed defect. An indirect member-pointer call `(object.*member)(value)` exposes a bound-member expression type, not a function prototype; the helper returns before inspecting the RHS `MemberPointerType` pointee. The compiler probe proves both default Windows modes destroy `Arg` in the callee, but the product falsely emits one caller-owned `temporary` destructor and no unresolved argument-destruction site. Explicit Itanium correctly owns that temporary in the caller. The coordinator inspected and independently reran both probes in `build/reviewer-phase1b`; all three compiler runs succeed and the mismatch is reproduced. Required correction: obtain the actual parameter prototype without resolving the variable's unknown method target; Windows modes suppress the caller destructor and retain unresolved argument/callee evidence, while Itanium retains the caller destructor. Cover both member-pointer invocation operators where applicable and preserve the existing free-pointer/lambda/operator/dependent-template controls.

The audit also found a low-severity documentation mismatch: virtual-base constructor/destructor edges are actually emitted from compiler evidence. Narrow the claimed limitation to missing virtual-base distinction/metadata and execution ordering; do not discard correct edges. Closure copy/move and unnamed constructors were judged explicit deferred coverage, and remain required before full Phase 1/release acceptance. Other bounded contracts passed the audit; freshly linked Phase 1A analyzer and gate probes pass 32/32 and 11/11, independently rerun by the coordinator. Agent2 is authorized only for these two corrections and related regressions/docs, followed by all-configuration/core-only verification and a frozen re-review handoff. The next adapter increment remains unauthorized.

Corrected handoff at 15:55: the report's "Re-review corrections" section records the member-pointer prototype fix, regressions for both `.*` and `->*`, and narrowed virtual-base wording. Only four product files differ from the original frozen snapshot (analyzer implementation, analyzer tests, callable fixture and analyzer documentation). Agent2 reports CTest 5/5 in all three configurations and Clang-disabled 3/3. The coordinator independently reran all nine executables: core 91/1,487, analyzer 19/1,390, smoke 8/67 in every configuration, with no skips. The source is frozen again while agent3 freshly links and checks the isolated three-mode/member-operator probes; the next adapter increment is still not authorized pending that verdict.

Phase 1B accepted at 15:59: agent3's corrected fresh-link probe passes both `.*` and `->*` in all three modes. Each Windows mode has zero caller destructors, two unresolved argument-destruction sites and two unresolved callee sites; Itanium has two caller temporary destructors, no unresolved argument-destruction sites and two unresolved callees. No mode invents `Mem::take` as a confirmed target. The coordinator independently reran this probe with zero failures. External-boundary and virtual-base controls remain intact, and the document now matches the actual base-edge behavior. No additional actionable defect remains in this bounded review. Closure copy/move, unnamed constructors and other explicitly pending full-Phase-1 coverage remain required.

### Real C++ context survey: next adapter work identified

The isolated `build/reviewer-fmt-context/fmt_context_probe.exe` reads the pinned database as data and calls only normalization and safety APIs; the coordinator inspected its source and independently reran it. Parsing succeeds with 51 commands and no diagnostics: 50 C++ entries (50 distinct command IDs) and one C entry outside the initial C++ scope. All 50 C++ entries identify their source and report normalization `ok`, but all are rejected by the separate safety gate. Normalizer `ok` is not evidence of a verified/invokable context.

Observed blocker counts: `--` in 50/50; `-Wno-deprecated-declarations` in 46/50; joined `-imsvc<gtest path>` in 46/50. No frontend analysis or compilation-database command execution occurred. All 51 command/directory/source records match the coordinator's input snapshot. The 31 distinct files (including C) include five files with alternative command profiles; those alternatives were not merged. These counts describe adapter readiness only, not graph precision/recall or retrieval quality, and do not permit dropping failed in-scope facts from a later benchmark.

Agent3's final census at 15:08 confirms 30 C++ source files and 50 unique C++ command IDs. Alternative-profile counts are `src/format.cc` 2, `src/os.cc` 5, `test/test-main.cc` 6, `test/gtest-extra.cc` 6 and `test/util.cc` 6. Variants include header-only, Unicode, posix-mock, defines and output identities. Four commands are blocked only by `--`; the other 46 have all three observed blockers. The input snapshot matches all 51 database records. Agent3 is preparing a bounded operand/end-of-options regression matrix for the later adapter fix; it is not modifying product code or advancing the implementation scope.

Plan a separate narrowly scoped adapter-compatibility correction after the current semantic increment: model the actual end-of-options boundary rather than merely adding `--` to an allowlist; retain source/operand classification and extra-input rejection for option-shaped filenames. Handle `-imsvc` as its real ordered system-include option, preserving its path and search semantics rather than silently converting it to a user include. Treat the observed warning option through a documented audited rule; do not broaden warning prefixes in ways that accept `-Wp,`, `-Wl,`, plugin/config inputs or unrelated unknown forms. Re-run the frozen 50-context census, positive/negative operand tests and invocation-safety regressions before any actual corpus analysis. Passing that future gate still will not prove frontend success or release accuracy.

Agent3 completed six concrete regression contracts at 15:14, using argument-print-only `clang-cl -###` probes in `build/reviewer-fmt-context/driver-semantics` (no frontend execution or product/corpus edits):

1. `clang-cl -TP -c -- -dash.cc`: recognize the sole option-shaped source after consuming the structural delimiter; do not retain the source as an unknown option in analyzer arguments.
2. `clang-cl -TP -c -- main.cc extra.cc`: retain the non-selected positional input as unsupported/unknown and reject the request, rather than silently dropping an extra frontend job.
3. `clang-cl -TP /I -- -imsvc -- -c -- main.cc`: the first two `--` tokens are literal include operands, respectively user and system; only the third starts positional input parsing.
4. Joined `"-imsvcsys one"` and separated `-imsvc "sys two"`, interleaved with `-I "user mid"`, preserve atomic paths, system status and original metadata order. Assert order within the system include group, not global cc1 ordering across distinct include search groups.
5. `clang-cl -TP -c -- @harmless.rsp main.cc`: response expansion precedes option parsing, even after `--`. Since expansion is unsupported, keep explicit degraded status and reject the whole request; do not reclassify `@...` as an ordinary positional source or accept solely because remaining arguments pass the gate. The scratch response file contained only `extra.cc`.
6. Combine the exact observed `-Wno-deprecated-declarations` with an unknown warning and `/clang:-load /clang:plugin.dll`: only the audited warning may be removed as diagnostic-only; unknown/forwarded options remain rejected. This negative contract was not executed as a compiler command.

These are requirements for the later adapter correction, not passing product tests. Joined versus separated normalized `-imsvc` representation is an implementation choice provided operand consumption, system semantics and relative ordering remain faithful.

Read-only agent2 design: `build/reports/adapter-design.md`. Coordinator representation decisions (not implementation authorization while Phase 1B review is open): use a precise structural disposition for an actual end-of-options delimiter; normalize the observed include to joined `-imsvc<value>` without changing its search group; preserve raw tokens/dispositions and command identities. An include-search-kind enum may replace the existing mutable `system` bool, with a derived system predicate if useful, rather than storing two independently mutable descriptions of the same classification. Existing quote/system/after groups should retain their real semantics with only directly related consumer/test changes. Adding `--` immediately before the separate entry path in our own **Clang** invocation is acceptable for both Clang driver modes, verified by invocation regressions, but does not determine how the original compiler parsed its raw command.

Additional coordinator driver evidence: native MSVC 19.44 `cl.exe /nologo /Zs -- -lcm-probe-source.cc` reports D9002 for both `--` and the option-shaped filename, then D8003 (no source filename), exit 2. No source or output file was created. Thus grouping `cl.exe` and `clang-cl` under `DriverFlavor::msvc` does **not** authorize interpreting `--` as a delimiter for both original compilers. The later correction must distinguish actual recognized compiler executables for Clang-only raw syntax, or conservatively reject unsupported native-cl cases. In particular, merely leaving native-cl `-imsvc` unknown while expanding the shared analyzer allowlist could accidentally accept an unfaithful context; test the entire normalization-plus-gate path. Do not invent response expansion or strip an option-shaped native-cl source into a verified input. Include paired `clang-cl`/`cl.exe` positive/negative cases; GNU-mode delimiter handling is separately verified. The existing fmt scope is specifically clang-cl and remains unchanged.

### Adapter compatibility correction (activated after 15:59 Phase 1B acceptance)

Owner: agent2 for minimal normalizer/gate/invocation, include metadata, tests and directly related docs. Agent3 reviews the frozen handoff and freshly rebuilds its context census afterward. Preserve all prior callable/identity/safety regressions and all raw command/profile identities. Do not edit PRD, corpus snapshots or other agents' diagnostics.

- Implement the six driver contracts above and paired native-cl negatives. Newly supported raw `--` syntax is limited to explicitly identified Clang-family executables whose driver semantics are verified; do not silently extend it to `gcc`, `g++`, `cc`, `c++` aliases or native `cl.exe` merely because they share a parser flavor. Unproven aliases remain conservatively rejected for this syntax. Internal analyzer invocation is always Clang and may use its own source delimiter independently.
- Consume operands before interpreting a delimiter; keep response inputs degraded before or after it; reject non-selected inputs and unknown/forwarded switches. Preserve native-cl behavior conservatively and ensure a new shared allowlist entry cannot bypass the original-driver restriction.
- Add faithful joined/separated `-imsvc` support for clang-cl, preserve search-group metadata and within-group ordering, and keep source/include paths atomic. Use one authoritative include-kind field instead of a redundant mutable system flag. Drop only the exact audited clang-cl warning token; no new broad warning/passthrough family allowance.
- Verify real header resolution through the system include directory and source-delimiter invocation with small safe fixtures. No compilation-database shell commands, response expansion, plugin/config execution or automatic project build.
- Re-run the fixed fmt 50-context normalization/gate census through a fresh linked diagnostic; expect 50/50 gate-ready C++ commands, still 50 command identities, 30 C++ files and all alternatives preserved. This is not corpus frontend/quality evidence. Use agent2-owned scratch for its check, leaving reviewer/coordinator artifacts untouched.
- Build and run affected core/analyzer/smoke suites in all three configurations and a Clang-disabled core. SDK provisioning code is unchanged, so no repeated SDK inventory/bootstrap investigation is required for this narrow change. Report exact results, remaining limitations and files, then freeze for independent review. No other semantic features, storage, provider spend, publication, commits or extra agents are authorized by this assignment.

Early integration boundaries identified during implementation (not yet product acceptance): after consuming `--`, a non-selected positional such as `-DSTEALTH=1` or `/Iextra` must not become an allowed option merely because the downstream gate sees only strings. Reject the whole request based on its positional role/explicit incomplete status, including option-shaped extra inputs. Separately, the coordinator's pinned argument-print-only probe `clang-cl -### /I @harmless.rsp -c main.cc` emits `-I extra.cc` because the harmless response file contains `extra.cc`: expansion also precedes option-operand consumption. A standalone `@...` used as `/I`/`-imsvc`'s operand must therefore remain an explicitly unsupported response input and cause rejection; joined `/I@literal` is a literal include spelling, not a response token. Agent2 received focused regressions for both boundaries; no response expansion or compilation-database execution is authorized.

Normalization spelling clarification: the coordinator independently verified that pinned `clang-cl -### /imsvclcm-system-probe -c main.cc` emits `-internal-isystem lcm-system-probe`. Therefore the implementation may reuse the existing common MSVC canonical `/` prefix for the joined `imsvc` replacement instead of adding a special branch solely to retain `-`. Original spelling, compiler-kind gating and internal-system semantics still remain mandatory. This supersedes the earlier preferred dash spelling, not any safety or search-order requirement.

Parallel read-only preparation: while agent2 owns this adapter implementation, agent3 is inspecting the remaining closure copy/move/assignment and unnamed-constructor compiler facts and identity boundaries in its own scratch. The coordinator is reviewing canonical signature/owner construction. Preparation must cover collision-safe local type identities (including two `consume(decltype(lambda))` overloads) without compiler-generated absolute paths/line numbers or changes to existing named-type golden IDs. This is future semantic planning only, not a second implementation assignment or acceptance of those remaining requirements.

Pinned compiler preparation (`build/reviewer-callable-remaining`) confirms that closure/unnamed-record constructor names and canonical parameter/self types contain source paths and coordinates; the coordinator reran the probe with an absolute input path. Disabling `AnonymousTagLocations` makes distinct lambda/anonymous-record overload parameter spellings identical, so neither that flag alone nor path regex stripping can provide identity. Reuse compiler-resolved local record/lambda identity atoms while preserving qualifiers and overload distinctions; keep existing named-type signatures/golden IDs unchanged. Namespace-scope lambda anchoring requires additional care: the current validator rejects a lambda `LocalScope` with an empty semantic owner chain even when its declarator enclosing ID is valid. This must be resolved without inventing a class/function owner for a namespace variable initializer.

The same preparation found a potential false-confirmed attribution, not merely absent hidden-member support: `VisitCallExpr` currently folds any operator call whose method parent is a lambda, including legal captureless copy/move assignment, into the lambda callable. Pinned AST plus source control flow confirms the route; runtime product reproduction awaits a frozen compatible build. The later semantic correction must fold **only** the actual closure call operator, and an assignment-only fixture must have zero enclosing-to-lambda execution calls. Actual assignments should eventually target referenced hidden assignment members with their own contribution/identity, alongside copy/move construction and anonymous-type construction. An explicit `copy()` invocation is a separate positive control. These remain full-Phase-1 requirements and are not accepted by the completed bounded Phase 1B fixtures.

Adapter handoff verification: `build/reports/adapter-handoff.md` reports the six contracts and extra positional/response-operand regressions. The coordinator independently reran all nine product executables with zero failures/skips: each configuration has core 100 cases / 1,998 assertions, analyzer 20 / 1,510 and smoke 8 / 67. Rebuilding the separate `build/coordinator-coreonly` initially encountered the known sandbox PATH/Path duplicate-key error; the identical normal-toolchain build succeeded, followed by CTest 1/1 and core 100 / 1,998. Agent2 acknowledges source/tests/docs/shared-build freeze. Agent3's independent audit is active, including a freshly linked 50-context census and unchanged-input check; the implementation author's reported 50/50 safe contexts are not yet independent evidence. No corpus frontend analysis has run.

Fresh independent census subsequently confirms 50/50 C++ gate-ready commands, 50 identities, 30 C++ files, 46 internal-system include observations and all five alternative-profile groups; snapshot comparison reports 51 entries and zero mismatches. The coordinator reran that diagnostic. The negative-input audit remains open: duplicate selected-source inputs are silently dropped as normal rather than rejected. Agent3's fresh isolated probe fails for repeated same spelling, `main.cc`/`./main.cc`, and a duplicate across the delimiter; pinned argument-print-only driver checks show two cc1 jobs. The coordinator independently reproduced the alias driver jobs and initial product failure. This is a multiple-input completeness boundary, not permission to run compilation-database commands.

The coordinator prepared and linked `build/coordinator-fmt-frontend/out/Release/fmt_frontend_probe.exe` in an isolated diagnostic tree. It requires one explicit command ID, stages only that unit, reports diagnostics/limits/counts and never publishes or merges profiles. It has **not** run corpus frontend analysis; use awaits adapter acceptance. Any later survey must retain failures and alternatives, and must not claim graph quality from frontend success.

Read-only follow-up design `build/reports/callable-identity-design.md` is not implementation authorization. Coordinator decisions: full stable-ID atoms with unambiguous encoding, fixed `(ctor)` for unnamed constructors, and the exceptional `lambda-decl-init` anchor only for an actual declarator-backed lambda with empty semantic owner chain. Preserve already valid namespace/class initializer lambda IDs rather than changing anchors solely for uniformity. The validator cannot inspect the entity behind an enclosing hash; closure members must instead carry their actual lambda owner component and explicitly use the lambda as local enclosing identity even at namespace scope. The first design's contrary transitive-validation statement needs correction. Canonical atomization must not reintroduce paths through template arguments or collapse distinct local named types; unsupported cases require truthful limits, not printable fallback promoted to identity. Both ordinary and explicit member-call syntax, including generic lambda specializations, must fold only the actual call operator; assignment/conversion do not execute the body by implication.

### Adapter audit correction: duplicate selected inputs

Agent3 holds bounded adapter acceptance for one medium-severity completeness defect: `positional()` silently drops a second matching source even though Clang schedules two jobs. The correction is agent2-owned and limited to source multiplicity handling, focused normalizer/analyzer regressions and directly related documentation. Count actual selected-source occurrences regardless of identical spelling, lexical/filesystem alias or position before/after `--`; a second source occurrence makes the context explicitly unsupported/degraded and must reject before frontend invocation. Apply the same invariant to source-taking `/Tp`/`/Tc` paths so they cannot bypass positional accounting. Do not mistake include/define operands that happen to equal the file name for source occurrences. Preserve single-source filesystem identity, raw arguments, command IDs and all 50 fmt contexts. Verify the three independent failing cases, ordinary and source-option mixed forms, positive operand controls, all three affected configurations and Clang-disabled core; freeze for agent3's fresh re-review. No callable implementation or corpus frontend execution starts during this correction.

Correction handoff at 16:38 is frozen. The coordinator independently reran all nine current executables: core 101 cases / 2,428 assertions, analyzer 20 / 1,552 and smoke 8 / 67 in each configuration, with zero failures/skips. Its separate rebuilt core-only tree passes CTest 1/1 and 101 / 2,428. Agent2 reports full CTest 5/5 per configuration, core-only 3/3, exercised case-alias subcase and unchanged 50/50 context census. Agent3's freshly linked focused re-review is now active; no acceptance is inferred from the handoff or passing tests alone. The coordinator relinked its isolated frontend-readiness executable against this corrected snapshot but has not run it.

### Prepared next semantic increment (not activated)

The independent read-only design review at 16:33 identifies four requirements for the later callable/local-type increment; these are proposed-design risks, not newly verified runtime product findings:

- Structural non-type template arguments need kind, canonical type and actual recursive value, not only their type's stable ID. `K{1}` and `K{2}` cannot collapse. If a value form is not supported in the bounded increment, expose a limitation and omit that unsupported identity rather than print an untrustworthy fallback; full template coverage stays pending.
- Integral non-type arguments need canonical type plus value: `template<auto>` specializations for `1` and `1u` must remain distinct with correctly resolved calls.
- Local-type-containing function pointer signatures preserve calling convention and exception specification as well as parameters, return type, variadic and qualifiers. Plain, `noexcept` and `__vectorcall` overloads are distinct; conversion target identities use the same rules.
- Keep the latest `lambda-decl-init` exception and existing valid anchors. Multiple lambdas in one declarator, e.g. `auto g = ([]{return 1;}, []{return 2;});`, preserve distinct ordinals and only the second closure is called by `g()`.

Use a small tagged/length-delimited canonical representation for local-type compositions if that is simpler than reproducing C++ declarator typography; unchanged non-local named types keep the established printer and golden IDs. Representation must distinguish every supported type constructor and qualifier, not merely remove paths. The coordinator also identified a source-level dependency to test: same-named local enums in separate blocks of one function may currently share the same name/zero ordinal linkage. Atomization cannot cure that by reusing a colliding type ID; verify and minimally correct its structural ordinal if reproduced. Preserve existing valid IDs, including closure destructors, wherever their underlying identity is unchanged. Do not expand this preparation into type-reference graphs, storage or general dataflow; those remain separate required work.

### Phase 1C callable/local-type correction (activated after 16:40 adapter acceptance)

Owner: agent2 for the analyzer, directly necessary identity/IR changes, fixtures/tests and implementation docs. Agent3 reviews the frozen handoff. The preceding design is advisory; the corrected decisions and requirements here are authoritative.

1. Fix the actual false-folding route: only a closure's real call operator (including its generic specialization) folds into the lambda callable. Cover `x()`, `x.operator()()`, generic forms and immediate invocation. Assignment-only fixtures have zero enclosing-to-lambda execution calls; assignment and conversion do not imply body execution. Keep actual ABI argument ownership and template evidence orthogonal.
2. Preserve referenced hidden closure default/copy/move constructors, copy/move assignment and conversion, with compiler-derived state, contribution provenance and truthful source anchors. Unused synthesized members remain absent. Keep the existing destructor identity; new members have distinct stable anchors. Calls inside actual synthesized constructor/member bodies belong to that hidden callable, not the enclosing function or lambda body. Unavailable synthesis is an explicit limitation, not a fabricated member call. Compiler-internal `__invoke` remains a stated boundary and calls through pointer variables remain unresolved.
3. Support unnamed-record constructors with fixed `(ctor)` and signature distinctions, anchored to the actual anonymous type. Verify default/copy/move construction and compiler-confirmed member initialization; preserve typedef-named record behavior.
4. Resolve namespace declarator initializer lambdas using the exceptional `lambda-decl-init` only when their actual semantic owner chain is empty. Preserve existing valid anchors and per-declarator ordinals. Explicitly anchor closure members to the lambda at namespace scope as well as local scope. Keep orphan/malformed key negatives and test two lambdas in one initializer plus exact target selection.
5. Give local/anonymous type leaves collision-safe stable-ID atoms. Preserve nested cv/ref/pointer/member-pointer/array/function/template structure, function ABI/noexcept attributes and type/value distinctions in template arguments. No absolute paths, source lines, body text, regex stripping or colliding raw fallback. Unsupported type/value forms or recursive identity dependencies produce precise limitations rather than invented identities; record every remaining form for later full-template/type work. Test the four design-review counterexamples above, common wrapper compositions and same-named local types, including nested-block enums if the suspected collision reproduces.
6. Verify copied-root and line-insertion stability, distinct overload targets, unchanged established named-symbol and closure-destructor IDs, three-run determinism, original-byte provenance, external referenced-only boundaries and multi-contribution publication/removal. A missing referenced target must not silently masquerade as complete support.

Run affected core/analyzer/smoke suites in Debug, Release and RelWithDebInfo plus Clang-disabled core, report exact results and remaining limits, then freeze. Do not rewrite the canonical schema, perform unrelated refactoring, relax safety, modify coordinator/reviewer/corpus artifacts or start storage/UE/deep/provider/publication work. Full type-reference graphs, all template/include forms, base metadata, allocation/deallocation, no-context behavior and every later PRD gate remain required.

Concurrent coordinator work uses a separately linked frozen diagnostic executable (SHA-256 `d36fe3ced24ffb6042d677b4e86248837735c885fbfc2dc33f7a884d4257ff16`) to survey each of the explicit 50 command IDs independently, without publication/profile merging. The first selected `src/format.cc` context completes with status ok, 1,459 symbols, 2,148 relations, 812 unresolved sites and 22 limitations; these counts are not correctness metrics. An initial sandbox process-start failure and a coordinator runner's missing-exit-code issue were corrected and rerun; neither is a product frontend failure. The first confirmed execution exits 0. Full survey outcomes will be recorded separately, preserving failures and timeouts.

Final corpus readiness result: `build/reports/fmt-frontend-readiness.md` and run `build/coordinator-fmt-frontend/runs/20260918-074238-803` record all 50 explicit C++ IDs, each process exit 0 and each result `ok/complete/no-errors`; no timeout or omitted C++ input. Ten warnings in five units, 29 distinct limitation messages (17–26 per unit) and unresolved sites (220–2,743 per unit) remain visible. All 51 source-hash observations and the database hash remain unchanged. At 16:47 agent3 independently verifies exact ID coverage, matching BEGIN/RESULT pairs, frozen executable hash, separate profile results and diagnostic interpretation. It notes that process exit summaries are in the terminal transcript/report rather than a machine-readable run manifest. This diagnostic is complete but is not graph precision/recall, retrieval quality, a clean-run performance measure or release acceptance. Phase 1C implementation continues independently.

Phase 1C early source review added three regression requirements: depth/unknown type locality must not fall through to path-bearing raw type printing; member-pointer class types must recursively preserve template/local arguments rather than qualified class name alone; semantically equal structural array values must canonicalize initialized elements and fillers consistently across TUs. These were delivered to agent2 during implementation. Draft source changes are not acceptance; verify the frozen handoff independently.

Frozen handoff at 17:19: `build/reports/phase1c-handoff.md`. The coordinator independently reruns all nine executables, zero failures/skips (core 102 cases/2,438 assertions, analyzer 22/2,132, smoke 8/67 in each configuration), and rebuilds its separate Clang-disabled tree (CTest 3/3 and core 102/2,438). Agent3's independent review is active, including unsupported-wrapper identity fallback, exact hidden-member/aggregate ownership and source provenance. No subsequent feature implementation is activated during this audit.

Independent negative probe during that audit reproduces an actionable unsupported-wrapper failure. Valid Clang C++ source `auto g = [] {}; void atomic_fn(_Atomic(decltype(g))* value) { (void)value; }` is analyzed with status ok and zero limits, but its signature contains `_Atomic((lambda at D:\\...\\atomic.cpp:1:10)) *` instead of a safe identity or explicit omission. The coordinator inspected the reviewer-owned read-only probe and independently reran it against the frozen libraries: exit 1 / FAILURES 1. The ordinary `decltype(g)*` control uses a stable lambda atom. Thus passing suites are insufficient and Phase 1C is not accepted. Await the completed audit before authorizing the bounded correction; preserve the frozen snapshot meanwhile.

Expanded independent probe (also rerun by the coordinator) confirms a second blocker group: `template<auto> Wrap` instantiated with typed null pointers loses the pointer type in raw printing. Two closure-pointer overloads collapse to one `(Wrap<nullptr>)` symbol with two definition locations; named `A*` versus `B*` null-pointer overloads collapse the same way. The copied/line-shifted atomic fixture changes its ID. In contrast, short/int/long-long integral controls remain three distinct overloads; explicit/parenthesized/generic explicit IILE flags, aggregate member-call original-byte anchor, structural value distinctions, unused closure-member absence and survivor-only contribution hash/empty cleanup pass. The fresh probe reports FAILURES 2 (groups), not broad acceptance.

Prepared correction requirements, pending audit completion/activation: make raw-printer fallback conditional on proven safe type/argument classification, not a default for unhandled wrappers. Unsupported local-leaf-containing wrappers must omit dependent identities with explicit limits, without path/line leakage. Typed-null template arguments must retain canonical type even for wholly named non-local pointees; merely checking for a local leaf is insufficient. Inspect Integral argument canonical types so local enums reach the existing atom encoder; preserve the demonstrated correct named/integral controls and established golden IDs. Add both reproduced negatives, named typed-null controls, local-enum composition, copied-root/line-shift stability and exact call-target regressions. Keep the correction limited to type/argument classification and directly necessary identity spelling/tests/docs; no unrelated callable rewrite, base/include implementation or other phase.

Agent3's completed audit confirms two High blocker groups and withholds Phase 1C acceptance. It independently validates the listed passing controls and frozen source hashes. Its additional recommendation is to pin the semantic aggregate member call's original closing-brace span/hash in the regression suite (the observed compiler-provided anchor is truthful, not itself a defect). Local-enum NTTP raw spellings are indistinguishable in the two-block probe, although distinct lambda ordinals prevent an ID collision in that particular fixture; record that distinction rather than claiming a reproduced enum collision. Non-local short/int/long-long integral controls also pass and must not be needlessly rewritten.

#### Phase 1C identity correction — activated after the two-High audit

Agent2 owns only the bounded correction above, related regressions and implementation documentation. For unsupported wrappers, recursively classify relevant contained types and treat unclassified forms as unknown, never proven non-local. An unsupported local-containing form must reach a truthful limit/omission path; do not invent support by stripping source spelling. `NullPtr` always requires canonical typed encoding, including named non-local pointees. `Integral` must inspect its canonical argument type so local-enum arguments are atomized; retain established safe non-local numeric spellings and golden IDs.

Regression acceptance: original and copied/line-shifted atomic fixtures have no leaked/bogus dependent symbol and explicit limits; ordinary pointer control stays supported/stable; closure-pointer and named-pointer null NTTP overload counts are exactly two each with exact calls to the correct distinct targets; nested local-enum arguments retain distinct stable type atoms; short/int/long-long and 1/1U controls remain distinct without gratuitous identity changes. Pin aggregate implicit member-call source byte/hash, rerun determinism and contribution removal, and preserve all existing callable/safety/identity tests. Run Debug/Release/RelWithDebInfo core/analyzer/smoke and Clang-disabled core, then freeze with exact handoff results. Do not edit reviewer/coordinator artifacts or start the prepared base/include increment. Agent3 will freshly relink and re-review after the correction handoff.

Coordinator draft check keeps this correction bounded: new atomic/vector/complex/non-prototype atom encoders are not part of this correction; required broader type work remains pending. Recursive locality detection is required so unsupported forms reach their truthful limit path. Do not whitelist all dependent types, or `DependentNameType` specifically, as leaf-like/proven non-local: pinned `TypeBase.h` explicitly stores its nested-name qualifier. `build/coordinator-type-classifier/dependent.cpp` is a valid compiler-checked example of `typename Box<decltype(namespace_lambda), T>::type*` retaining a concrete closure inside that qualifier. Agent2 must verify actual canonical-key behavior, safely inspect relevant contained types or report an unsupported composite, and preserve genuine template-parameter leaf controls. This is a source/API-supported risk in the draft, not yet a separately reproduced product-key failure.

Fresh-session review preparation at 19:46: `build/reviewer-fresh/initial-review.md` records a new read-only audit and five compiler-valid fixture families, copied and line-shifted roots, and an observer prepared for fresh linkage after freeze. It confirms that the current draft already has `qualifier_locality`; the old R3 whitelist description is historical. Two further source risks were sent to the new implementer: unclassified expression/template arguments treated as non-local, and named nested types whose enclosing specialization carries a closure. Neither is yet an independently reproduced product failure. The coordinator additionally compiler-checks `build/coordinator-fresh/dependent-member.cpp` against the pinned SDK: dependent member-pointer qualifiers can carry a closure even when a record declaration is unavailable. Current product behavior, precise limitations and safe named controls must be verified before the new handoff. The next increment remains inactive.

New frozen correction handoff at approximately 20:00: `build/reports/phase1c-correction-handoff.md`. The implementer reports actual pre-fix product-key leaks for R3 and expression/array-bound/nested-owner/dependent-member-pointer routes; these are corrected with truthful omission for unsupported composites, preserving named controls. The coordinator verifies all 11 handoff hashes, reruns all nine executables (core 102/2,438, analyzer 23/2,295, smoke 8/67 in every configuration; no failures/skips), and verifies unchanged source hashes across the run. Its separately rebuilt Clang-disabled Debug tree passes CTest 3/3 and core 102/2,438. Durable coordinator evidence: `build/coordinator-fresh/phase1c-2000/`. The new reviewer is now freshly linking the frozen Release libraries for final independent probes. Phase 1C remains unaccepted pending that review; no next increment is active.

Fresh independent review at approximately 20:05 reproduces a further High blocker: `Wrap<&Holder<decltype(g)>::value>` where `Holder<T>` has `inline static int value = 0`. The `Declaration` argument branch checks its parameter type and function-local declaration status, but misses the declaration's enclosing specialization. The coordinator independently confirms with the freshly linked reviewer probe (SHA-256 `9986891157b2bf7580ebc84ab907f33c4fe7bab34cf90bf242957ce7ca2f64cd`): both warning-free original and copied/seven-line-shifted inputs analyze successfully with no limits, yet raw signatures contain absolute lambda paths and different IDs. Evidence: `build/coordinator-fresh/declaration-repro/`. Phase 1C remains rejected; preserve the frozen source until the reviewer finishes the bounded audit, then assign a narrow correction and re-review.

The coordinator also reproduces a second defect in that same `Declaration` branch: two overloads using `Wrap<&target<int>>` and `Wrap<&target<double>>` collapse into one `(Wrap<&target>)` symbol with two definition locations. The closure-type specialization pair collapses likewise. Source and current-probe output: `build/coordinator-fresh/function-declaration-repro/`. This requires preserving declaration specialization identity or an explicit unsupported boundary; checking enclosing owner locality alone cannot fix it. The implementer is assigned read-only correction preparation while the final audit finishes; product writes/builds remain frozen.

The completed fresh audit (`build/reviewer-fresh/final-review.md`) rejects the 20:00 handoff for H3/H4 only: 608/615 oracle checks pass, seven observations fail for H3, and a separate independent H4 run confirms both overload collisions and wrong caller targets. Prior H1/H2/R3/C1–C4 and callable/provenance/contribution controls pass; all 13 source/library hashes remain unchanged. At approximately 20:14 the coordinator activates a narrow Declaration-argument correction, selecting truthful omission for identities whose specialization value cannot be represented without folding. No new encoder is authorized. Isolated safe named declaration controls (including `Wrap<&Holder<int>::value>`) keep their IDs; unsupported function-specialization arguments must yield explicit declaration-NTTP limits, no bogus symbols or confirmed targets, and truthful unresolved evidence. Preserve structural `TemplateParamObjectDecl` handling, numeric/typed-null/golden IDs and full copy/line-shift regression coverage. Repeat the complete matrix, submit a new handoff preserving the rejected one, then freshly relink and re-review. No next feature increment is activated.

The new Declaration correction handoff (`build/reports/phase1c-declaration-correction-handoff.md`) is frozen at approximately 20:30. The coordinator verifies all 13 source/test/doc/library hashes and reruns all nine executables: core 102/2,438, analyzer 23/2,406 and smoke 8/67 in every configuration, with unchanged source hashes and no failures/skips. Evidence and a separate source snapshot are preserved in `build/coordinator-fresh/phase1c-2030/` and `build/coordinator-fresh/phase1c-2030-source/`. The independent reviewer is freshly linking its v2 observer for 27 original/copied/line-shifted runs. Omitted specialization-valued callees currently produce an explicit analysis limitation and no confirmed edge, but no per-call unresolved record; this bounded identity correction does not close the broader unresolved-evidence requirement. Phase 1C acceptance and direct-base/include activation remain pending.

### Concurrent next-step preparation (not activated)

Agent3 completed a read-only direct-base/include API review at 16:55. The coordinator checked the pinned Clang 22 base access/source APIs, conditional callbacks and dependency-directive scanner header. Use structured outer-to-inner branch paths rather than synthesized condition strings; preserve effective access plus whether it was explicitly written. Base details belong to contribution evidence, not a last-writer relation property. Active compiler-resolved include observations and inactive lexical directives must remain distinct; missing callbacks alone do not prove inactivity after an aborted parse. No inactive target guessing or transitive edges. Final activation still requires a bounded contract after Phase 1C acceptance; no implementation is authorized by this preparation.

Pinned Graft 0.18.0 source prerequisites are recorded in `build/reports/graft-baseline-prerequisites.md`, including archive and lock hashes, lifecycle/update-check side effects and Git-visible input scope. After source inspection, `npm ci --ignore-scripts --no-audit --no-fund` installed 74 platform-applicable packages using only a build-local cache; the lock hash is unchanged. Explicit TypeScript/viewer builds pass; an initial esbuild sandbox EPERM was resolved by rerunning the identical approved local viewer build. No prepare/postinstall/telemetry stamping, Graft CLI execution, host wiring, model calls or comparative measurement has run. Parser runtime and CLI state/input isolation remain unverified. This preparation does not satisfy any quality, token-savings or release gate.

Subsequent isolated runtime check: `build/baselines/graft-cpp-runtime-probe.mjs` imports only Graft's generic extractor, loads the C++ WASM grammar and extracts an in-memory two-function sample. Exit 0, three nodes and one raw caller-to-helper edge; file-only fallback is explicitly rejected by assertions. C++ generic-parser readiness is verified, but full CLI/native-parser loading and CLI state/input isolation remain unverified. No corpus or comparative benchmark ran.

The later native extractor import initially failed for missing `tree-sitter-kotlin` 0.3.8 binding. Its reviewed, explicit local build used pinned node-gyp and official Node 24.16.0 headers with a build-local cache. An MSBuild long-path FileTracker error was resolved by rebuilding the same generated solution with `TrackFileAccess=false`; no parser/algorithm source was patched. The rebuilt native extractor import passes. CLI execution/state/input isolation and all comparative metrics remain pending; exact commands, warning and binary hash are in the baseline prerequisite report.

An isolated full Graft CLI `--help` startup subsequently passes with an allowlisted child environment, build-local user/profile/temp state, no provider credentials and no `.graft` state creation. The first sandbox child check failed before Graft startup; the identical approved external-sandbox check passes. Help does not invoke ordinary-command preAction, so update-cache/output/input-scope controls remain required before build/query. No corpus comparison or model operation ran.

Later structural-only CLI prerequisite smoke passes on the coordinator's isolated one-file toy input: one file parsed, three nodes, one call edge and one card. The runner verifies an isolated child homedir, prevents outer Git-root inheritance, seeds only a fresh diagnostic-local update cache, requests no ignore-file edits and keeps output outside input. Source hash and file list and the update-cache bytes remain unchanged. No `--deep`/`--lsp`, model keys, user-global wiring, existing fmt input changes or benchmark runs. Exact evidence and limitations are in `build/reports/graft-baseline-prerequisites.md`; this is not a release quality/usage/performance claim.

For the later include contract, also preserve repeated visits to the same non-guarded header under different macro states within one analysis unit. Same file/offset does not guarantee the same active branch or resolved macro operand on each visit. Retain distinct compiler observations rather than last-writer overwrite; lexical directives in never-entered or aborted regions have no inferred target. The future fixture should include a repeated-header/toggled-macro case in addition to independent-TU contribution removal.

Coordinator compiler-only API check: `build/coordinator-include-scanner/include_scanner_probe.cpp` links pinned Clang 22 `clangLex`/`clangBasic`, not product libraries. The fresh executable exits 0: all five include directives survive, including a literal `#if 0` branch, elif/else alternatives, guarded include and a macro operand after backslash-CRLF continuation. Scanner token offsets recover original bytes including the trailing comment and line terminator in this fixture. This validates the proposed lexical source mechanism for these cases only; it does not resolve inactive targets, prove full conditional semantics or activate product implementation.

### Direct-base/include increment (activated after fresh Phase 1C acceptance)

Phase 1C acceptance: `build/reviewer-fresh/declaration-final-review.md` independently accepts the agreed bounded correction with no reproduced blocker. Newly linked v2 executable SHA-256 `BA9A277498D2528F464BBC6F445E9EF4F1E0EAF163B4FBA4A3C47A5DA3AB6198` passes all 27 runs and 1,002 checks; all 13 frozen hashes remain unchanged. The coordinator verifies that report and evidence alongside its nine passing executable runs. Previous failed handoffs remain preserved. Full specialization-value support, unsupported composites and per-call unresolved completeness remain pending, not waived.

Owner: `lcm-impl-fresh` implementation; `lcm-review-fresh` independent scratch preparation and frozen-handoff review. The coordinator releases the product freeze only for the six-point assignment below. This closes the two named data gaps, not all of Phase 1 or graph/query/UE requirements. `build/reports/base-include-design.md` is advisory; the following coordinator decisions bound the implementation. Reuse existing generated build trees or create uniquely named ones; do not delete prior evidence.

1. Add direct-base details to contribution-owned relation evidence: compiler-effective access, whether access was written, virtual flag and zero-based lexical ordinal. Preserve full original base-specifier span/hash. Keep direct compiler-resolved targets only; no transitive edges or name/prefix/pure-virtual interface classification. Include these details in evidence equality/order/hash and survivor-only contribution tests.
2. Add an authoritative contribution-aware direct-include fact collection with add/publication/remove/hash support, independent of symbol node lifetimes. Do not invent file symbols solely to fit include directives into current symbol relations. Preserve directive kind, raw operand and operand kind, optional expanded spelling, activity, resolution state, original directive location and structured outer-to-inner conditional branch path (including preceding siblings for elif/else).
3. Combine pinned compiler lexical scanning with actual preprocessor observations. Active/resolved internal observations carry the actual repository target. Inactive observations never acquire a target by filesystem/name guessing. Indeterminate/aborted or unvisited regions remain truthful; a missing callback is not sufficient proof of inactive state. Scan failures produce explicit limitations/diagnostics rather than an invented complete result. Retain repeated visits under changing macro states within one TU without last-writer overwrite.
4. Keep external resolved locators in private dependency observations, not public repository paths or ranking edges. A contribution fact may use an opaque dependency key linked to those private observations; no external body/API indexing. Existing forced-include/PCH dependencies must not become fabricated source directives. A header-guard-skipped file can still have an active, compiler-resolved include directive. Transitive closure and ranking remain later query work, not stored edges.
5. Fixtures: explicit/default base access plus virtual/nonvirtual lexical order and exact spans; nested if/elif/else in two contexts; literal if-zero; macro include with backslash-CRLF; guarded repeated include; same non-guarded header entered twice with toggled macros and distinct targets; missing include/aborted parse; external target privacy; main→direct→leaf without main→leaf fabrication. Include original bytes/hash, three-run determinism and two-TU contribution removal/survivor-only hash checks. Preserve existing callable/identity/safety regressions and established symbol IDs.
6. Verify affected core/analyzer/smoke in Debug, Release and RelWithDebInfo plus Clang-disabled core; report exact results/limitations/files and freeze. No storage, general preprocessor evaluator, type-reference graph, allocation/deallocation, no-context fallback, UE, provider or publication work in this increment. Every excluded release requirement remains pending rather than waived.

Compiler-only scanner boundary found during this increment: `build/coordinator-fresh/include-scanner-cases/result.log` shows the pinned dependency scanner returns success but no directives for `%:include`/`%:if`/`%:endif`. The actual pinned frontend accepts `digraph.cpp` (exit 0), enters `present.hpp`, and does not enter the absent inactive header. Empty prior conditional siblings, split-keyword CRLF, interleaved comments and raw-string false directives pass the scanner probe. Scanner success also does not diagnose an unterminated comment/conditional; frontend diagnostics remain authoritative. Do not silently promote scanner success into lexical completeness. Use a compiler-lexer check with explicit limitation for unsupported directive syntax when faithful recovery exceeds this bounded implementation; preserve real private observations and do not invent a complete condition path or inactive target. This is an API feasibility finding, not a reproduced product failure or full include-syntax acceptance.

The fresh reviewer's compiler-only observer passes nine runs and 165 checks, including effective/written access, full commented base spans, separate FileID visits at the same header offset, and active header-guard-skipped directives. Missing-include fatal diagnostics, `#error` and many parser errors can still be followed by real `InclusionDirective` callbacks. Such observed directives remain active; do not blanket-downgrade everything after an error. Only unmatched directives without proven skipped-range evidence remain indeterminate. This preparation is not product acceptance.

Independent preparation is now frozen in `build/reviewer-fresh/base-include/prepared-review.md`. Follow the advisory exact-byte convention: the full logical directive excludes its final LF/CRLF terminator, retains intermediate backslash-continuation CRLF, trailing comment and all other original bytes. Condition/operand text does not accidentally absorb that final terminator. On the pinned Windows target, `#import` is an unsupported Microsoft type-library import with diagnostics and no include callback; do not fabricate a header resolution. `include_next` has actual compiler callbacks. A missed modeled directive in a file invalidates the scanner's claim of complete conditions; conservatively omit that file's public direct-include facts with a precise limit unless incompleteness is explicitly represented. Private observed dependencies remain available. Prepared oracle details and corrected exploratory assumptions are preserved in the reviewer report.

### 2026-09-22 resumed blind audit and ownership correction

The new GPT-5.6-sol reviewer examined current code/test/build artifacts without implementation reports or prior verdicts. Its fresh Release build and standalone probe reproduce two findings: (1) compiler-resolved implicit-template bases such as `struct D : G<int>` are omitted with a limitation; this also exists in the pre-increment source and remains a required template/inheritance gap; (2) `FactSet::add_direct_include` accepts an empty `analysis_unit`, unlike its sibling contribution-owned insertion APIs, allowing an orphan include. The coordinator reran the probe independently and reproduced all three failed assertions representing these two findings. Other exercised ordinary/macro/explicit-specialization base metadata and generated-include semantic contracts pass. Neither the full direct-base requirement nor Phase 1 is accepted.

Agent2 is now authorized only to reject empty include ownership before mutation and add a regression for rejection, unchanged state/hash, and valid contribution removal. Verify affected core/analyzer/smoke suites in all three configurations and Clang-disabled core, then freeze for code-only re-review. SDK scripts are unchanged and their full matrix passed earlier today; they need not be repeated for this guard. Preserve all prior evidence. Template-base support and skipped-callee unresolved-site correction remain separate pending increments; read-only design/reproduction is allowed in implementer-owned scratch.

### Prepared template-base correction (awaiting ownership re-review)

Owner: Agent2; Agent3 reviews the frozen actual code without implementation explanations. Correct the independently reproduced missing edge for compiler-resolved template bases. Fold instantiated class bases to the compiler-selected primary or partial pattern and retain concrete template arguments on each contribution-owned evidence. Multiple bases selecting one pattern share a relation key but retain distinct arguments, lexical ordinals, access/virtual metadata and original spans. Never create public nodes for every instantiation or store transitive edges.

Append truthful TemplateUse values for partial implicit use and explicit-instantiation declaration/definition; preserve existing enumerator values/strings and function-call behavior. For explicit instantiations, the target's canonical template role identifies primary versus partial selection. This only describes base-use evidence; complete declaration indexing remains a separate requirement. Ordinary and explicit-specialization base identities stay stable. Unsupported argument encoding or unreachable patterns yield explicit limitations rather than empty-argument confirmed edges or raw source-location spelling.

Verify primary, selected partial, multiple instantiations of the same pattern, explicit instantiation declaration/definition, ordinary/explicit-specialization controls, external referenced-only targets, representable local arguments, unsupported/dependent negatives, exact spans/hash, contribution survival and determinism. Run affected core/analyzer/smoke in Debug/Release/RelWithDebInfo and Clang-disabled core; unchanged SDK discovery need not repeat. Freeze for independent review before any later semantic increment. The skipped-callee correction is still pending separately.

Ownership correction accepted after code-only re-review on 2026-09-22: empty/populated insertion rejection preserves collection and hash; valid add/remove lifecycle passes the freshly relinked independent probe. Coordinator affected-suite reruns pass all three configurations and Clang-disabled core (core 105/2,469; analyzer 26/2,663; smoke 8/67). Only the separate template-base finding still fails in the broad probe. The prepared template-base correction above is now activated; product freeze is released only for that bounded assignment.

Template-base correction accepted by the code-only GPT-5.6-sol review on 2026-09-22: the fresh independent probe reports `template_base_edges=1`, `template_base_limit=0`, `failures=0`, including copied-root/five-line-shift local and lambda arguments, selected partials, multiple same-pattern bases, external boundaries, unsupported negatives, contribution survival and explicit-instantiation declarations before/after the use. The coordinator independently reran the final probe with exit 0 and unchanged source hashes. Explicit-instantiation evidence reflects Clang final TU AST classification rather than chronological source-order state. Core/analyzer/smoke counts are 106/2,482, 28/2,759 and 8/67 respectively; all three configurations and separate Clang-disabled core pass. The owner guard remains accepted. This closes the reproduced bounded findings, not whole Phase 1 or every template-argument form. Next prepared work is the separately reproduced skipped-callee unresolved-site gap; it has not been implemented or activated. Current recovery record: `2026-09-22-Progress-and-Resume.md`.
